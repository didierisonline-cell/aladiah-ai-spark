import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.49.1";
import { requireAdmin } from "../_shared/adminGuard.ts";
const corsHeaders = {"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};

const courseData = {
  "title": "AI Solutions Architect",
  "description": "Master enterprise AI architecture — TOGAF, cloud-native AI patterns, microservices, data architecture, security architecture, and the complete capstone of designing a Fortune 500 AI platform. 10 deep modules combining the depth of AWS Solutions Architect Professional with AI-specific production patterns.",
  "translations": {
    "es": {
      "title": "AI Solutions Architect",
      "description": "Master enterprise AI architecture — TOGAF, cloud-native AI patterns, microservices, data architecture, security architecture, and the complete capstone of designing a Fortune 500 AI platform. 10 deep "
    },
    "fr": {
      "title": "AI Solutions Architect",
      "description": "Master enterprise AI architecture — TOGAF, cloud-native AI patterns, microservices, data architecture, security architecture, and the complete capstone of designing a Fortune 500 AI platform. 10 deep "
    },
    "de": {
      "title": "AI Solutions Architect",
      "description": "Master enterprise AI architecture — TOGAF, cloud-native AI patterns, microservices, data architecture, security architecture, and the complete capstone of designing a Fortune 500 AI platform. 10 deep "
    },
    "zh": {
      "title": "AI Solutions Architect",
      "description": "Master enterprise AI architecture — TOGAF, cloud-native AI patterns, microservices, data architecture, security architecture, and the complete capstone of designing a Fortune 500 AI platform. 10 deep "
    },
    "ar": {
      "title": "AI Solutions Architect",
      "description": "Master enterprise AI architecture — TOGAF, cloud-native AI patterns, microservices, data architecture, security architecture, and the complete capstone of designing a Fortune 500 AI platform. 10 deep "
    },
    "ja": {
      "title": "AI Solutions Architect",
      "description": "Master enterprise AI architecture — TOGAF, cloud-native AI patterns, microservices, data architecture, security architecture, and the complete capstone of designing a Fortune 500 AI platform. 10 deep "
    }
  },
  "modules": [
  {
    "title": "Module 1: AI Solutions Architecture Fundamentals — Patterns, Tradeoffs, and the Architect's Mindset",
    "desc": "Solutions architects translate business requirements into technical systems. The architectural thinking process, decision frameworks, tradeoff analysis, the C4 model for AI systems documentation, and the architect's role at AWS, Google, and Stripe.",
    "lessons": [
      { "title": "1.1 What AI Solutions Architects Do", "desc": "The AI Solutions Architect role bridges business strategy and technical implementation. Unlike software engineers (who build) or data scientists (who model), architects design the complete system: how data flows from source to model to business outcome, how components connect and communicate, how the system behaves under failure, and how it scales from 1,000 to 1,000,000 users. Core architect responsibilities: requirements analysis (translate business needs into technical constraints), technology selection (evaluate and recommend tools, frameworks, and platforms), architecture documentation (create diagrams and ADRs that guide implementation), risk assessment (identify technical risks and mitigation strategies), and cross-team communication (explain technical decisions to engineering, product, and executive stakeholders). The architect's mindset: architects optimize for system-level properties (reliability, scalability, maintainability, cost) rather than component-level properties (algorithm efficiency, code elegance). Case study: the AWS Solutions Architect role — AWS SAs are measured on customer success, not code shipped. Their output is whitepapers, reference architectures, and technical guidance that enables customers to succeed with AWS." },
      { "title": "1.2 Architectural Thinking: Requirements, Constraints, and Quality Attributes", "desc": "Good architecture starts with understanding requirements deeply — especially the unstated ones. Functional requirements: what the system must do (predict churn with 90% accuracy, serve predictions in under 100ms). Non-functional requirements (quality attributes): how well the system does it. The PASS quality attributes for AI systems: Performance (latency: p99 < 100ms, throughput: 10K req/sec), Availability (99.9% uptime = 43 minutes downtime/month), Scalability (handle 10x traffic growth with linear cost increase), Security (GDPR compliance, SOC2 Type II). Constraints: budget ($50K/month cloud spend limit), team skills (Python expertise, no Go), existing infrastructure (AWS-only), timeline (production by Q3). The architect's job: satisfy functional requirements while meeting quality attributes within constraints. ADRs (Architecture Decision Records): document the decision, the context, the options considered, and the rationale — the architecture memory of why things are built as they are. Case study: how Stripe documents every major architectural decision with ADRs — their engineering blog's detailed technical posts are public ADRs." },
      { "title": "1.3 The C4 Model: Documenting AI System Architecture", "desc": "The C4 model (Simon Brown) provides four levels of abstraction for communicating software architecture. Context (Level 1): the system in its environment — users, external systems, the AI system as a black box. Container (Level 2): deployment units within the system — ML serving API, feature store, training pipeline, model registry. Component (Level 3): major components within each container — fraud detection model, feature computation service, prediction cache. Code (Level 4): classes and functions — typically too detailed for architecture discussions. For AI systems, an additional model card diagram (inspired by C4 but specific to ML): the model context (what data the model consumes, what predictions it produces, who consumes those predictions). Diagramming tools: C4-PlantUML (code-based diagrams version-controlled with code), Mermaid (Markdown-embedded diagrams in documentation), Lucidchart (collaborative visual diagramming). Living documentation: architecture diagrams as code in the repository — updated when architecture changes, not as an afterthought. Case study: how AWS architecture reference diagrams (the canonical visual language for cloud architecture) follow C4-inspired conventions." },
      { "title": "1.4 AI Architecture Patterns: Lambda, Kappa, and Event-Driven", "desc": "Recurring architectural patterns solve recurring problems. Lambda architecture (Nathan Marz, 2011): batch layer (accurate, slow — nightly Spark jobs compute ML features), speed layer (approximate, fast — streaming Flink jobs compute real-time signals), serving layer (merges batch and speed results). Used by: Twitter, Netflix (before Kappa migration). Problems: maintaining two codebases for the same business logic, eventual consistency between layers. Kappa architecture (Jay Kreps, 2014): single streaming pipeline replaces both batch and speed layers — reprocess historical data by replaying Kafka with increased parallelism. Advantages: one codebase, no consistency issues. Prerequisites: Kafka with sufficient retention for historical reprocessing. Event-driven architecture: AI systems as event consumers and producers — model predictions published as events consumed by downstream systems. The strangler fig pattern: incrementally replace legacy batch ML with real-time streaming, one feature at a time. Case study: Netflix migrated from Lambda to Kappa architecture for their recommendation features — eliminating 40% of infrastructure code by removing the batch layer." },
      { "title": "1.5 Tradeoff Analysis: The Architect's Core Skill", "desc": "Every architectural decision involves tradeoffs — the architect's job is to make the right tradeoffs explicit and choose consciously. The fundamental tradeoffs in AI systems: consistency vs. availability (Brewer's CAP theorem — you can't have both during a network partition), latency vs. throughput (optimizing for one typically hurts the other), freshness vs. cost (more frequent retraining = fresher model = higher compute cost), accuracy vs. explainability (complex models are more accurate but harder to explain), build vs. buy (custom solutions optimize for specific needs but require maintenance). Tradeoff documentation format: the decision, the options evaluated, the tradeoffs for each option, the criteria used to choose, and the chosen option with rationale. Anti-pattern: the architect who never commits — documenting infinite tradeoffs without recommending a direction. The good architect recommends the best option for the specific context while being transparent about what's sacrificed. Case study: Airbnb's engineering blog documents their decision to build a custom ML feature store (Zipline) vs. using an open-source alternative — transparency about the tradeoffs is what makes the post valuable." },
      { "title": "1.6 Architecture Review Process: RFC, Design Reviews, and Feedback", "desc": "Architecture decisions affecting multiple teams require structured review. RFC (Request for Comments) process: engineer proposes architecture change in a document, stakeholders review asynchronously, synchronous discussion meeting, decision recorded in ADR. RFC document structure: problem statement, proposed solution, alternatives considered, migration plan, rollback plan. Design review meeting: architect presents, reviewers ask questions, decision made or follow-up actions assigned. Review criteria: does it meet quality attributes? Are risks identified? Is the migration path clear? Is it maintainable by the team that will own it? Architecture review board: senior architects review high-impact decisions (new data stores, new service boundaries, fundamental pattern changes). The review anti-patterns: design by committee (too many reviewers, no decision), rubber stamp review (reviewer approval without genuine examination), architect veto (senior architect blocks without clear rationale). Case study: Google's design doc culture — every significant engineering change starts with a design document reviewed by relevant engineers. The design doc template is publicly available and widely copied." },
      { "title": "1.7 Module 1 Assessment: Architecture Analysis", "desc": "Analyze a provided AI system architecture description. Identify: quality attribute tradeoffs, architectural pattern used (Lambda/Kappa/event-driven), the 3 highest risks in the design, the 2 architectural decisions that most need ADR documentation, and one alternative architectural pattern that would solve the stated problem differently. Write a 1-page architectural recommendation." }
    ]
  },
  {
    "title": "Module 2: Designing AI Data Architectures — From Raw Data to Model-Ready Features",
    "desc": "The data architecture is the foundation of every AI system. Data lake vs. data warehouse vs. lakehouse decisions, ELT pipeline design, feature engineering architectures, data freshness SLA design, and the data architectures at Uber, Airbnb, and Netflix.",
    "lessons": [
      { "title": "2.1 Data Architecture Decisions: Lake, Warehouse, or Lakehouse?", "desc": "The first architectural decision for any AI system is where and how data lives. Data warehouse (Snowflake, BigQuery, Redshift): structured data, SQL interface, optimized for analytical queries, expensive at large scale, limited support for unstructured ML data (audio, images, text). Data lake (S3 + Spark): cheap storage, flexible schema, supports all data types, complex query interface, no ACID transactions (data corruption risk). Data lakehouse (Delta Lake + Databricks, Iceberg + Spark): combines lake economics with warehouse capabilities — ACID transactions, schema enforcement, SQL interface, support for unstructured data. Decision framework: team skill set (SQL-native → warehouse, Python/Spark-native → lakehouse), data types (structured only → warehouse, mixed → lakehouse), query frequency vs. storage volume (high ratio → warehouse, low ratio → lake), ML use cases (complex ML feature engineering → lakehouse preferred). Most AI-first companies land on lakehouse — it handles structured, semi-structured, and unstructured data while providing the ACID guarantees needed for feature engineering. Case study: Databricks migrated from separate data lake and warehouse to lakehouse — eliminating the $2M/year 'data copy tax' of maintaining both systems." },
      { "title": "2.2 ELT Pipeline Architecture: Ingestion Patterns and Data Flow Design", "desc": "ELT (Extract, Load, Transform) vs. ETL (Extract, Transform, Load) — modern data architecture uses ELT. Why ELT: cheap cloud storage makes it cost-effective to load raw data first and transform in the warehouse/lakehouse, raw data preserved for debugging and reprocessing, transformation logic version-controlled in dbt rather than scattered across ETL scripts. Ingestion pattern selection: full refresh (small dimension tables, <1M rows, simple), incremental batch (cursor-based on updated_at, 90% of use cases), CDC (Change Data Capture via Debezium — real-time database replication for sub-minute latency), streaming (Kafka → Flink — sub-second for real-time features). The fan-out pattern: one ingestion pipeline feeds multiple consumers (data warehouse, feature store, real-time ML, analytics) — avoiding redundant source system load. Idempotency requirement: every ELT pipeline must be safely re-runnable (INSERT OR REPLACE, not INSERT — duplicate runs must produce correct results). Data contracts: formal schema agreements between producers and consumers prevent breaking changes from silently corrupting ML models. Case study: Stripe's data pipeline architecture: CDC from Postgres → Kafka → Flink → Delta Lake — millisecond ingestion latency for their fraud detection features." },
      { "title": "2.3 Feature Engineering Architecture: Batch, Streaming, and On-Demand", "desc": "Feature engineering architecture determines model freshness and feature consistency. Three feature computation tiers: batch (Spark on daily schedule — cost-efficient for slowly changing features like 30-day rolling averages), streaming (Flink on Kafka — sub-minute freshness for rapidly changing signals like 5-minute transaction counts), on-demand (computation at serving time — real-time context like current time of day, request metadata). Architecture decision for each feature: what is the required freshness? What is the acceptable compute cost? Is the feature stateful (requires historical data) or stateless (computable from request context alone)? The feature dependency graph: features computed from other features — upstream failures cascade downstream. Architecture requirement: feature pipeline isolation (failure of one feature view should not block unrelated features). Training-serving consistency: batch features computed in dbt must use identical logic as online features computed in Flink — the feature store enforces this by versioning feature definitions. Case study: DoorDash's feature architecture: 80% batch (daily Spark), 15% streaming (Flink with 30-second freshness), 5% on-demand — balancing freshness requirements against compute cost." },
      { "title": "2.4 Data Freshness SLA Design: Matching Freshness to Business Requirements", "desc": "Not every feature needs real-time freshness — and over-engineering freshness increases cost and complexity unnecessarily. Freshness requirement analysis: what decision does this feature inform? How quickly does the underlying reality change? What is the business cost of stale data? Examples: user demographics (name, address) — weekly refresh sufficient, no real-time requirement. Purchase history — daily refresh acceptable for most recommendation use cases. Active session data — second-level freshness required for fraud detection. SLA tiers: T+24h (next-day batch, cheapest), T+1h (hourly batch), T+5m (near-real-time streaming), T+30s (real-time streaming), T+1s (on-demand). The freshness cost curve is exponential: T+24h may cost $100/month in compute; T+30s may cost $10,000/month for the same feature. Architecture responsibility: design freshness SLAs with business stakeholders, then architect the pipeline to meet those SLAs at minimum cost. Case study: Spotify's recommendation features: listening history (T+1h acceptable, batch), now-playing context (T+1s required, on-demand), genre affinity (T+24h, daily batch) — right-sizing freshness by use case saves $3M+ annually." },
      { "title": "2.5 Data Mesh Architecture: Distributed Ownership at Enterprise Scale", "desc": "Data mesh (Zhamak Dehghani, 2019) distributes data ownership to domain teams — eliminating the centralized data platform bottleneck at scale. The four principles: domain ownership (payments team owns payment features, not the data platform team), data as product (each domain publishes high-quality data products with SLAs and documentation), self-serve data platform (infrastructure team provides tools for domains to build without data engineering expertise), federated governance (organization-wide standards with domain-level implementation flexibility). Architectural implications: each domain deploys its own data pipeline infrastructure (using platform-provided templates), publishes data products via standardized interfaces (Delta Lake tables in a shared catalog, REST APIs, Kafka topics), and owns its own SLAs. The platform team's role in data mesh: build the self-serve infrastructure (storage, compute, catalog, quality tooling) that enables domains to build without deep infrastructure expertise. Anti-patterns: data mesh as data lake (same centralized infrastructure with domain labels), data mesh without governance (domains diverging on incompatible standards). Case study: JPMorgan's data mesh: 50 domain teams each own their data products — time-to-data for new ML use cases reduced from 6 weeks to 4 days." },
      { "title": "2.6 Multi-Cloud and Hybrid Data Architecture: Regulatory and Performance Considerations", "desc": "Enterprise AI systems increasingly span multiple clouds and on-premises infrastructure. Multi-cloud data architecture drivers: data residency requirements (EU GDPR requires EU data to stay in EU — some data must be processed in EU clouds), disaster recovery (data replicated across clouds provides recovery from cloud-provider outages), best-of-breed services (BigQuery for analytics, S3 for ML training data, Azure for enterprise identity). The data gravity problem: large datasets are expensive to move — compute must come to the data, not vice versa. Architecture patterns for multi-cloud: data lakehouse per cloud with read-only cross-cloud views (Delta Sharing, Iceberg open format), event streaming hub (Confluent Cloud spans multiple clouds for event distribution), federated query (Trino, Databricks Unity Catalog query data wherever it lives without copying). Hybrid cloud for regulated industries: sensitive data on-premises, ML training on public cloud (differential privacy or synthetic data enables this), model serving on-premises for latency. Case study: Capital One's cloud strategy: most workloads on AWS, some regulated data on-premises, BigQuery for analytics — multi-cloud gives leverage without excessive complexity." },
      { "title": "2.7 Module 2 Assessment: Data Architecture Design", "desc": "Design the complete data architecture for a healthcare AI company building a patient readmission risk model. Requirements: 50 hospital EMR sources (HL7 FHIR format), HIPAA compliance required, EU data residency for European hospitals, model predictions must use features no older than 1 hour. Specify: ingestion architecture per source type, lakehouse design with medallion layers, feature engineering architecture for 20 key features with freshness tier justification, multi-cloud data residency design, and data governance controls for HIPAA compliance." }
    ]
  },
  {
    "title": "Module 3: ML System Architecture — Training Pipelines, Model Serving, and Scalable Inference",
    "desc": "ML system architecture spans the lifecycle from training data to production prediction. Training pipeline architecture, model serving patterns, inference optimization, multi-model systems, and the ML architectures at Google, Meta, and OpenAI.",
    "lessons": [
      { "title": "3.1 ML Training Pipeline Architecture: Components and Orchestration", "desc": "Production ML training pipelines have five architectural layers. Data layer: validated training data retrieved from feature store or data warehouse, versioned with DVC, split into train/validation/test with temporal split (not random — prevents leakage). Feature layer: feature engineering applied consistently between training and serving (same code path as online feature computation), feature importance analysis to reduce dimensionality. Training layer: distributed training framework (PyTorch DDP for neural networks, XGBoost distributed for gradient boosting), hyperparameter optimization (Katib or Optuna), experiment tracking (MLflow or W&B). Evaluation layer: holdout set evaluation, fairness metrics, adversarial robustness testing, comparison against production champion model. Deployment layer: model registration (MLflow Model Registry), quality gate enforcement (accuracy > threshold, latency < target), staged rollout (shadow → canary → production). Orchestration: Kubeflow Pipelines or Airflow coordinates all layers — the pipeline as code, version-controlled, reproducible. Case study: Google's Vertex AI Pipelines is Google's productization of their internal ML training pipeline architecture — each layer is a separate Vertex AI component." },
      { "title": "3.2 Model Serving Architecture: Synchronous, Asynchronous, and Batch", "desc": "Model serving architecture depends on latency requirements and throughput needs. Synchronous serving (REST/gRPC API): user waits for prediction — required for interactive applications (fraud detection during checkout: <200ms required), recommendation at page load. Architecture: load-balanced serving fleet, auto-scaling on RPS, connection pooling to feature store, prediction cache for repeated requests. Asynchronous serving (queue-based): prediction request enqueued, result fetched later — appropriate for non-interactive use cases (email send-time optimization, weekly churn predictions). Architecture: SQS/Pub-Sub queue, worker fleet with auto-scaling on queue depth (KEDA), result stored in DynamoDB/Redis, client polls or receives webhook. Batch serving: generate predictions for all entities on schedule — offline scoring for use cases that don't need real-time (next-day email targeting, weekly user segmentation). Architecture: Spark batch job on daily schedule, predictions written to data warehouse, downstream systems consume from warehouse. The serving mode selection framework: latency requirement (interactive → sync, background → async/batch), request volume (high → sync with autoscaling, low/scheduled → batch), prediction reuse rate (repeated requests → cache, unique requests → no cache). Case study: Netflix uses all three serving modes: sync for homepage recommendations, async for email targeting, batch for weekly content affinity scores." },
      { "title": "3.3 Microservices vs. Monolith Architecture for AI Systems", "desc": "Should a prediction service be part of a larger application or a dedicated microservice? Microservice advantages for ML: independent deployment (ML team deploys model updates without coordinating with application team), independent scaling (scale prediction service to 100 replicas while keeping other services at 3), technology independence (ML team uses Python/Triton, app team uses Go/gRPC), isolated failure (prediction service outage doesn't take down the entire application). Microservice disadvantages: network latency (inter-service calls add 1-10ms), operational complexity (service discovery, distributed tracing, network policies), data consistency (distributed transactions are hard). Monolith advantages for ML: zero network overhead (in-process model inference: <1ms), simpler operations (one deployment, one service to monitor), ACID transactions for prediction logging. Decision framework: team independence required (microservice), latency budget < 5ms for predictions (monolith or in-process), model updates frequent and independent (microservice), team skill in distributed systems (required for microservices). Case study: Shopify embedded their product recommendation model directly in the storefront monolith — in-process inference eliminates network overhead for a latency-sensitive checkout flow." },
      { "title": "3.4 Multi-Model Architecture: Ensembles, Cascades, and RAG Systems", "desc": "Production AI systems increasingly combine multiple models. Ensemble architecture: parallel predictions from N models aggregated by a combiner (weighted average, stacking meta-model). Serving architecture: request fan-out to N model services, aggregation service combines results, timeout handling for slow models (return partial ensemble if any model exceeds latency budget). Cascade architecture: cheap classifier routes requests to appropriate model tier — cheap model handles 80% of requests, expensive model handles 20% requiring deeper analysis. Architecture benefit: 3x cost reduction vs. serving all requests with the expensive model. RAG (Retrieval-Augmented Generation) architecture: query → embedding model → vector search → retrieved context → LLM → response. Component scaling: the vector search layer must scale independently from the LLM layer (different resource profiles). Multi-model coordination: when models are interdependent (model A output feeds model B), the architecture must handle failure gracefully — if model A fails, model B must have a fallback input. Case study: Google's AI Overviews in Search: a retrieval system (vector search over web index), a generation model (Gemini), and a safety classifier — three independent models coordinated by an orchestration layer." },
      { "title": "3.5 Inference Optimization: Quantization, Batching, and Caching Architecture", "desc": "The three levers for reducing inference cost and latency. Quantization: reduce model weight precision from FP32 (4 bytes/weight) to FP16 (2 bytes) to INT8 (1 byte) — 4x memory reduction, 2-3x throughput increase with <1% accuracy loss for most models. Architectural decision: quantize at model registration time (TensorRT compilation), serving infrastructure selects quantized or full-precision model based on accuracy requirements. Dynamic batching architecture: serving layer accumulates requests for a configurable window (10-100ms), processes as batch — GPU utilization improves from 20% (single request) to 85% (batch of 32). Tradeoff: batch wait time increases latency by window_size (up to 100ms). Prediction caching architecture: cache predictions for recently seen inputs (Redis, TTL = feature freshness window). Cache key: hash of model version + input features. Cache hit rate measurement: high hit rate (>50%) = significant cost savings; low hit rate (<10%) = cache overhead not worth it. Case study: Hugging Face reduced their hosted model inference costs by 60% through INT8 quantization + dynamic batching — making open-source model hosting economically viable." },
      { "title": "3.6 Architecture for LLM Applications: Context Management, Streaming, and Cost Control", "desc": "LLM application architecture differs fundamentally from traditional ML serving. Context window management: LLM inference cost scales with context length — architecture must manage context size. Strategies: context compression (LLMLingua — 3x compression with minimal quality loss), sliding window (maintain only last N turns), selective context (retrieve and include only relevant prior context). Streaming architecture: LLM tokens generated one at a time — streaming via Server-Sent Events (SSE) or WebSockets provides progressive display (first token in <1s vs. waiting 10s for complete response). Architecture components: LLM inference service (vLLM for throughput, direct API for simplicity), streaming proxy (handles SSE connection, buffers tokens), client-side progressive rendering. Cost control architecture: token budget per request (max_tokens = 500 — hard limit on output tokens), model routing (GPT-4o-mini for simple queries, GPT-4o for complex — routing model classifies complexity), prompt caching (Anthropic/OpenAI prompt caching API reduces input token cost for repeated system prompts by 90%). Case study: Intercom's AI customer service architecture: LLM router classifies query complexity, routes 85% to GPT-4o-mini, 15% to GPT-4o — achieving 70% cost reduction vs. all-GPT-4o serving." },
      { "title": "3.7 Module 3 Lab: Design an End-to-End ML System Architecture", "desc": "Design the complete ML system architecture for a real-time product recommendation system: training pipeline (Kubeflow components, MLflow tracking, model quality gates), feature architecture (batch features from dbt + streaming features from Flink), serving architecture (synchronous API with caching + async batch pre-computation), ensemble of 3 models (collaborative filtering + content-based + context-aware), inference optimization (INT8 quantization + dynamic batching), and failure handling (fallback to popularity-based ranking). Produce C4 context and container diagrams." }
    ]
  },
  {
    "title": "Module 4: Cloud Architecture for AI — AWS, GCP, and Azure Reference Architectures",
    "desc": "AI architectures are built on cloud primitives. AWS and GCP reference architectures for ML, managed AI services vs. self-managed, cloud cost optimization for AI workloads, and the cloud architecture patterns used at enterprise scale.",
    "lessons": [
      { "title": "4.1 AWS AI Reference Architecture: SageMaker, Bedrock, and the AI Stack", "desc": "AWS provides the most complete managed AI service portfolio. SageMaker: managed ML platform — SageMaker Studio (notebook environment), SageMaker Training (managed distributed training), SageMaker Pipelines (ML workflow orchestration, Kubeflow-compatible), SageMaker Model Registry (model versioning and governance), SageMaker Endpoints (managed model serving with auto-scaling). SageMaker economics: managed service premium (~30% over raw EC2) justified for teams without Kubernetes expertise; engineering cost savings typically exceed compute premium. Amazon Bedrock: managed LLM API gateway — access Claude, Llama, Titan through a single API with enterprise security (VPC endpoints, no data leaving your account, CloudWatch logging). The AWS AI reference architecture: S3 (data lake) → Glue (ETL) → SageMaker Feature Store → SageMaker Pipelines → SageMaker Model Registry → SageMaker Endpoints → CloudWatch (monitoring). Alternative architecture: S3 → dbt on Redshift → Feast on EKS → Kubeflow Pipelines → MLflow on RDS → custom serving on EKS. Case study: Intuit built TurboTax AI (200M+ predictions annually) on SageMaker — managed platform allowed their ML team to focus on models rather than infrastructure." },
      { "title": "4.2 GCP AI Reference Architecture: Vertex AI and the Google ML Stack", "desc": "Google Cloud's Vertex AI consolidates all ML services into one platform. Vertex AI Platform: Workbench (managed JupyterLab), Datasets (managed dataset versioning), Training (managed custom training with AutoML option), Feature Store (managed Feast-compatible feature store), Model Registry (model management with evaluation), Endpoints (managed serving with traffic splitting), Pipelines (Kubeflow Pipelines v2 backend, Vertex AI SDK). BigQuery ML: train ML models directly in SQL — for data analysts who know SQL but not Python. Cloud Run: serverless containers for ML inference — scale to zero between requests, ideal for low-traffic models. The GCP AI reference architecture: Cloud Storage (data lake) → BigQuery (data warehouse + ML) → Vertex AI Feature Store → Vertex AI Pipelines → Vertex AI Model Registry → Vertex AI Endpoints → Cloud Monitoring. TPUs: Google's custom ML accelerators — 4x performance per dollar vs. A100 for specific model architectures (Transformers, CNNs), available exclusively on GCP. Case study: Spotify uses GCP + Vertex AI for their ML platform — BigQuery for feature engineering, Vertex AI Pipelines for orchestration, TPUs for recommendation model training." },
      { "title": "4.3 Managed vs. Self-Managed AI Infrastructure: The TCO Decision", "desc": "Every AI architecture decision has a build-buy-manage tradeoff. The three options: managed services (SageMaker, Vertex AI — provider operates infrastructure), self-managed on cloud (Kubeflow on EKS, MLflow on EC2 — you operate infrastructure on cloud VMs), self-managed on-premises (Kubernetes on your own hardware). TCO analysis framework: compute cost (managed services carry 20-50% premium over raw compute), engineering cost (self-managed requires 2-3 platform engineers per major component), operational risk (managed services provide SLA, self-managed requires on-call coverage), lock-in risk (managed services create vendor dependency). Managed services make sense when: team size < 20 ML engineers (engineering cost > managed service premium), time-to-production is critical (managed services are operational in days vs. weeks for self-managed), operational expertise is limited (managed services include 24/7 operations). Self-managed makes sense when: >50 ML engineers (economies of scale), unique requirements that managed services can't meet, cost optimization is critical at scale. Case study: Stripe chose self-managed Kubernetes + open-source ML stack — at their scale (100+ ML engineers), the compute cost savings from self-management exceed the engineering cost of running the platform." },
      { "title": "4.4 Multi-Region AI Architecture: Global Serving and Data Residency", "desc": "Serving AI predictions globally requires multi-region architecture. Latency requirements drive region selection: a São Paulo user calling a us-east-1 model endpoint experiences 130ms network latency before any compute begins. Multi-region serving patterns: active-active (model deployed in all regions, global load balancer routes to nearest), active-passive (primary region serves all traffic, failover to secondary on incident), edge deployment (model on CDN edge nodes for sub-10ms inference). Global load balancing: AWS Global Accelerator (anycast routing — requests routed to nearest healthy region), Cloudflare Workers (edge inference for lightweight models), Google Cloud Global External Load Balancer (anycast + intelligent routing). Data residency for AI: EU GDPR requires EU user data to stay in EU — inference for EU users must use EU-resident feature stores and prediction logging. Architecture pattern: deploy model + feature store + prediction logging in each required region, global model registry with regional artifact replication (S3 Cross-Region Replication). Case study: Booking.com's multi-region AI: prediction APIs in 8 AWS regions, global P99 inference latency reduced from 380ms to 87ms after activating multi-region serving." },
      { "title": "4.5 Cloud Cost Optimization Architecture for AI: FinOps Patterns", "desc": "AI cloud costs grow faster than any other infrastructure category — architectural decisions have outsized cost impact. The three AI cost levers: compute (right-size instances, use spot, maximize utilization), storage (lifecycle policies, compression, deduplication), networking (minimize cross-region data transfer, use VPC endpoints). Spot instance architecture for ML training: Kubernetes Karpenter provisions spot instances automatically, FUSE checkpointing saves training state to S3 every 15 minutes — interruption causes <15-minute setback. Savings Plans for steady-state: compute savings plans cover 80% of baseline serving compute at 40% discount. Storage tiering: S3 Intelligent-Tiering automatically moves cold training data to cheaper tiers — saves 40-60% on inactive training data stored for reproducibility. Egress cost reduction: VPC endpoints eliminate data transfer charges for S3, SageMaker, and ECR access from within VPC (saves $4,500/month at 100TB/month). The FinOps architecture review: quarterly architecture review specifically for cost optimization — identify highest-cost components and evaluate alternatives. Case study: Grubhub reduced their ML infrastructure costs by 55% through spot instance migration + S3 Intelligent-Tiering + right-sizing — without changing model accuracy or latency." },
      { "title": "4.6 Cloud Security Architecture for AI: IAM, Encryption, and Network Isolation", "desc": "AI systems on cloud require specific security architecture. IAM for ML workloads: training jobs assume IAM roles via IRSA (EKS) or Workload Identity (GKE) — no static credentials in code. Least privilege: training role can read training data bucket only (not all S3), serving role can read model artifacts + call feature store (not write to data sources). Encryption: S3 default encryption with KMS CMK (all training data and model artifacts encrypted at rest), TLS for all API calls (in transit), feature store Redis TLS encryption. Network isolation: training jobs in private subnets (no internet access — all dependencies preloaded), VPC endpoints for S3/ECR/SageMaker (traffic stays on AWS backbone), Security Groups: model serving pods allow only ingress from load balancer. Secrets management: AWS Secrets Manager or HashiCorp Vault for API keys (Anthropic API, OpenAI API, database credentials) — never in environment variables or Kubernetes ConfigMaps. Audit trail: CloudTrail logs all API calls (who accessed which training data, which model was deployed when) — satisfies EU AI Act logging requirements. Case study: HSBC's AI cloud security architecture: private EKS clusters, KMS encryption for all ML artifacts, CloudTrail + Security Hub for continuous compliance monitoring." },
      { "title": "4.7 Module 4 Assessment: Cloud AI Architecture Design", "desc": "Design the cloud architecture for a fintech startup's fraud detection system. Requirements: process 100K transactions/second, predictions in <50ms, 99.99% availability, PCI-DSS compliance, EU data residency for European users. Compare AWS-native (SageMaker + managed services) vs. cloud-agnostic (Kubernetes + open-source) architectures across cost, operational complexity, time-to-production, and lock-in risk. Produce the recommended architecture with diagram and TCO estimate for 3 years." }
    ]
  },
  {
    "title": "Module 5: Real-Time AI Architecture — Streaming Systems, Event-Driven Design, and Low-Latency Serving",
    "desc": "Real-time AI requires sub-second latency across the entire pipeline. Kafka architecture for AI, Flink for stateful stream processing, low-latency serving patterns, and the real-time architectures at Uber, Stripe, and DoorDash.",
    "lessons": [
      { "title": "5.1 Real-Time AI Requirements Analysis: Defining Sub-Second Latency Budgets", "desc": "Not all AI systems need real-time — and over-engineering for real-time is expensive. Latency budget analysis: decompose the total user-facing latency into its components. Example fraud detection budget (150ms total): network (20ms), API gateway (5ms), feature retrieval from Redis (5ms), model inference (30ms), response serialization (5ms), network return (20ms) — available for all other processing: 65ms. Every millisecond matters at high throughput: 50ms average inference at 100K RPS = 5,000 seconds of compute per second — 10ms reduction = 17% capacity increase. Defining the latency SLO: p50, p95, p99, p99.9 — why each percentile matters. The 99.9th percentile represents 1 in 1,000 requests — at 100K RPS, 100 requests/second experience worst-case latency. Architecture implications: p99.9 requires eliminating garbage collection pauses, JVM warm-up time, and cold-start latency — favoring compiled languages (Go, Rust, C++) or pre-warmed JVMs. Case study: Stripe's payment API latency target: p99 < 300ms globally — every architecture decision evaluated against this constraint." },
      { "title": "5.2 Kafka Architecture for Real-Time AI: Topics, Partitions, and Consumer Groups", "desc": "Apache Kafka is the foundation of real-time AI data pipelines. Architectural decisions for Kafka in AI systems: topic partition count (determines maximum consumer parallelism — partition count ≥ max consumer count needed), replication factor (3 for production — tolerate 2 broker failures), retention period (how long to keep events for replay — 7 days for feature reprocessing, 30 days for audit), compacted topics (for latest-value semantics — entity state tables rather than event logs). Consumer group design: each consuming service (feature computation, audit logging, downstream systems) has an independent consumer group — parallel consumption without interference. Schema Registry: Avro or Protobuf schemas registered centrally — schema evolution with backward compatibility ensures consumers don't break when producers add fields. Dead Letter Queue (DLQ): events that fail processing routed to DLQ topic — manual review and replay rather than blocking the main consumer. Case study: LinkedIn's Kafka cluster: 7 trillion messages per day across 4,000 brokers — the largest Kafka deployment, documented in their 2022 engineering blog." },
      { "title": "5.3 Apache Flink for Stateful Stream Processing: Windows, State, and Exactly-Once", "desc": "Flink enables stateful computations over event streams — the ML feature computation backbone. Session-based architecture: Flink JobManager (coordinates execution), TaskManagers (execute tasks, maintain state). State backends: HashMap (in-memory, fastest, limited by heap size), RocksDB (disk-backed, terabytes of state, 2-3x slower reads). Checkpoint architecture: Flink snapshots state to S3 every 60 seconds — recovery from S3 snapshot after failure means reprocessing only the last 60 seconds of events (not full replay). Exactly-once with Kafka: Flink Kafka source + checkpoint + Kafka sink with 2-phase commit — no event processed more than once, no event missed. Window operations for ML features: tumbling windows (discrete hourly buckets), sliding windows (rolling 30-minute window updated every 5 minutes), session windows (activity sessions with inactivity gap timeout). Late event handling: watermarks define the expected latency of late events — process late events that arrive within the watermark window, drop events that arrive after. Case study: Alibaba's Flink deployment: 4 trillion events/day for real-time recommendation and fraud detection — the largest Flink deployment, Alibaba is the primary Flink committer." },
      { "title": "5.4 Low-Latency Feature Serving Architecture: Redis, In-Process Caches, and Read-Through", "desc": "Feature retrieval is on the critical path of every real-time prediction — architecture must minimize latency. Redis architecture for feature serving: Redis Cluster (horizontal sharding, 6 nodes minimum for HA), connection pooling (maintain persistent connections from feature server — avoid connection establishment overhead), pipeline commands (batch multiple feature lookups in one network round trip). The latency breakdown for Redis feature retrieval: connection pool checkout (0.1ms), network to Redis (0.5ms), Redis hash lookup (0.1ms), network return (0.5ms) = 1.2ms total. In-process cache: local LRU cache in the serving pod (most-recently-requested entities cached in pod memory — zero network latency for cache hits). Two-level cache architecture: in-process cache (hit rate 30-60%, 0ms, capacity limited by pod memory) → Redis cache (hit rate 90%+, 1-2ms, scalable). Read-through pattern: cache miss triggers automatic feature store lookup and cache population — transparent to the model. Case study: Twitter's real-time feature serving architecture: in-process Caffeine cache (30% hit rate) + Redis Cluster (65% hit rate) = 95% of feature lookups served from cache — sub-millisecond average feature retrieval latency." },
      { "title": "5.5 Event-Driven AI Architecture: Triggers, Reactions, and Autonomous Pipelines", "desc": "Event-driven architecture enables AI systems to react to business events autonomously — without polling or scheduled jobs. Core pattern: business event published to Kafka → multiple AI consumers react independently. Examples: 'user completed purchase' event → fraud model scores (synchronous, blocks checkout), recommendation model updates (async, updates next session), churn model re-scores (async, updates CRM). The event-driven advantage: producers and consumers are decoupled — adding a new AI consumer requires no changes to producers. Choreography vs. orchestration for AI pipelines: choreography (each service reacts to events, no central coordinator — simpler, harder to debug), orchestration (central workflow coordinates AI pipeline steps — more visible, creates coupling). Event schema evolution: AI consumers must handle schema changes gracefully — optional fields, backward-compatible additions, versioned event types. Idempotency requirement: AI consumers must handle duplicate events safely (Kafka at-least-once delivery) — store processed event IDs in Redis to deduplicate. Case study: Shopify's checkout risk scoring: 'checkout initiated' event triggers fraud model, inventory availability model, and shipping time model simultaneously — event-driven fan-out parallelizes independent AI evaluations." },
      { "title": "5.6 Real-Time AI Failure Modes and Resilience Patterns", "desc": "Real-time AI systems fail in ways that batch systems don't. Failure modes: feature store unavailability (can't retrieve features → can't make predictions → fallback required), model inference timeout (prediction takes too long under load → timeout → fallback), Kafka consumer lag (consumer falls behind producer → stale features → model accuracy degrades), cascading failures (slow feature store causes inference timeout, causes upstream timeout, causes downstream service failure). Resilience patterns: circuit breaker (stop calling a failing service, return fallback immediately — Istio DestinationRule), bulkhead (isolate failure — separate thread pools for feature retrieval and model inference), timeout with fallback (inference timeout → return cached or rule-based prediction), graceful degradation (feature store unavailable → serve model with available features, accept lower accuracy). The fallback hierarchy: full ML prediction (all features available) → partial ML prediction (some features cached) → rule-based decision → default safe response. Chaos engineering for real-time AI: inject feature store latency (100ms instead of 2ms) — validate that circuit breaker activates and fallback serves correctly. Case study: Stripe's payment risk architecture: 4-tier fallback ensures a risk score is always returned — real-time ML → cached ML score → statistical model → rule-based → approve-with-flag." },
      { "title": "5.7 Module 5 Assessment: Real-Time AI Architecture Design", "desc": "Design the real-time architecture for a ride-sharing surge pricing system. Requirements: price updates within 5 seconds of demand change, process 1M location events per minute, p99 latency < 200ms for price lookup, 99.99% availability. Design: Kafka topic architecture for location events, Flink streaming job for demand feature computation, Redis serving architecture for price lookup, Kubernetes serving deployment with autoscaling, circuit breaker configuration, and fallback pricing strategy. Include latency budget breakdown and failure mode analysis." }
    ]
  },
  {
    "title": "Module 6: Enterprise AI Architecture — Integration, Legacy Systems, and Organizational Patterns",
    "desc": "Most AI architects work in enterprises with complex existing systems. Legacy integration patterns, API gateway design, enterprise data governance, change management for AI systems, and the enterprise AI architectures at JPMorgan, Walmart, and Siemens.",
    "lessons": [
      { "title": "6.1 AI Integration Patterns: Strangler Fig, Sidecar, and Anticorruption Layer", "desc": "Integrating AI with enterprise legacy systems requires specific patterns. Strangler Fig pattern for AI: incrementally replace legacy rule-based systems with AI — route 10% of requests to AI model, 90% to legacy rules, compare outcomes, gradually increase AI percentage as confidence grows. Benefits: zero cutover risk, empirical validation before full migration, rollback is simple percentage change. Sidecar pattern: attach AI enrichment to existing services without modifying them — a sidecar container intercepts requests, calls the AI model, and augments the response. Used for: adding AI-generated summaries to existing content APIs, scoring existing customer records without touching the data warehouse. Anticorruption Layer (DDD): translate between legacy data models and AI-native data models without polluting either. The ACL handles impedance mismatch (legacy: customer ID is a 10-digit integer with business logic in the ID format; AI system: needs UUID + clean metadata). Case study: a major bank's credit scoring AI integration used the Strangler Fig pattern over 18 months — gradually replacing a 30-year-old SAS scoring model while maintaining regulatory continuity." },
      { "title": "6.2 API Gateway Design for AI Services", "desc": "AI APIs require gateway capabilities beyond standard API management. Rate limiting for AI: per-user token budgets (LLM APIs charge by token — unlimited usage creates runaway costs), per-service concurrency limits (prevent a single consumer from monopolizing GPU capacity), differentiated limits by tier (enterprise customers get higher limits than free tier). AI API versioning strategy: model version in the URL (/v1/predict vs. /v2/predict) enables gradual migration — clients explicit about which model version they call. Request routing: route to different model versions based on header (X-Model-Version: shadow for shadow deployments), A/B testing at the gateway level (10% of requests to new model version). Authentication for AI APIs: API key management (rotation, scoping, audit), OAuth 2.0 for service-to-service (client credentials flow), JWT validation at gateway (eliminates per-service auth logic). Observability at the gateway: log every request (input hash, model version, latency, cost) — the audit trail required by EU AI Act for high-risk AI. Case study: Anthropic's API gateway handles 100M+ daily requests — rate limiting, authentication, and routing are all gateway responsibilities, not model responsibilities." },
      { "title": "6.3 Data Governance Architecture: Lineage, Catalog, and Access Control at Enterprise Scale", "desc": "Enterprise AI data governance requires architecture for discoverability, trustworthiness, and compliance. Data catalog architecture: DataHub or Apache Atlas as the central metadata store, ingestion connectors for all data sources (BigQuery, Snowflake, dbt, Airflow, Spark), lineage collection (OpenLineage standard — integrates with Spark, dbt, Airflow, Flink), search and discovery (full-text search across all dataset metadata). Column-level lineage architecture: trace any column in any Gold table back through every transformation to its source system column — OpenLineage facets capture this at the Spark job level. Data access control architecture: attribute-based access control (ABAC) — policies based on data sensitivity, user role, and purpose. Row-level security: BigQuery row-level security policies, Databricks Unity Catalog row filters — users see only rows they're authorized for. PII detection pipeline: automated PII scanning of all new datasets (AWS Macie, Presidio) → tag sensitive columns → apply appropriate masking policies. Case study: LinkedIn's DataHub deployment: 2M+ datasets, 10,000+ tables, column-level lineage tracked by OpenLineage — the platform that inspired the open-source release has scaled to this level internally." },
      { "title": "6.4 AI Change Management: Versioning, Rollback, and Organizational Coordination", "desc": "AI system changes have organizational impact that pure software changes don't. Coordinated change patterns: model updates (ML team owns, can deploy independently if serving contract unchanged), feature schema changes (data engineering team owns, requires coordination with ML team), serving infrastructure changes (platform team owns, requires coordination with all consumers). The AI change taxonomy: backward-compatible (adding optional features, improving model accuracy without changing output schema — deploy independently), breaking (changing prediction schema, removing features — requires coordinated deployment with consumers), experimental (shadow deployment — no consumer coordination required). Release coordination for AI: change calendar (shared calendar of planned AI changes, consumers subscribe to notifications for services they depend on), compatibility window (maintain backward compatibility for N versions — consumers have time to migrate), API versioning (consumers can pin to a specific model version during migration). Blue-green deployment for enterprise AI: test in staging environment identical to production (same feature store data, same request distribution), validate against integration test suite, switch production traffic atomically. Case study: Meta's AI change management: their model serving infrastructure supports 1,000+ simultaneous model versions — consumers pin to stable versions while experimenters use latest." },
      { "title": "6.5 Enterprise AI Security Architecture: Zero Trust, DLP, and Third-Party Risk", "desc": "Enterprise AI security must address threats from inside and outside the organization. Internal threat model: employees sending company data to external LLM APIs (Samsung incident — engineers sent proprietary chip designs to ChatGPT). DLP (Data Loss Prevention) for AI: classify corporate data sensitivity, enforce policies at the API gateway level (requests containing PII or confidential data tagged by DLP proxy → blocked from external LLM APIs, logged for audit). Third-party LLM risk management: data processing agreements with LLM providers, contractual prohibition on training on enterprise data, enterprise API plans that guarantee data isolation. Zero-trust for internal AI services: every service-to-service call authenticated (mTLS), authorization at each call (OPA policy evaluated per request — not just at perimeter). AI system audit trail: log every prediction request with input hash, model version, and timestamp — the audit evidence for regulatory inquiries. Corporate AI policy: establish acceptable use policy for AI tools (what data can employees enter into external AI tools, what is prohibited), publish and train all employees. Case study: after Samsung's 2023 ChatGPT data leak, they banned external AI tools and deployed an internal LLM — the architecture decision was forced by a security incident that could have been prevented by DLP controls." },
      { "title": "6.6 Architecture for AI Regulatory Compliance: GDPR, EU AI Act, and Financial Services", "desc": "Regulatory compliance shapes AI architecture — requirements must be designed in, not bolted on. GDPR architecture requirements: right to erasure (model must be retrained without data of deleted user, or differential privacy must guarantee non-memorization), purpose limitation (data used for one AI purpose cannot be repurposed without consent — architectural data lineage tracks usage), data minimization (AI systems may only collect data necessary for stated purpose). EU AI Act high-risk system architecture requirements: complete logging of system operation (every prediction with inputs, model version, timestamp — 6 months minimum retention), human oversight mechanism (API endpoint to pause/override/disable AI decision-making), technical documentation (model card + training data documentation + architecture documentation automated from MLflow and DVC). Financial services AI architecture requirements: SR 11-7 model risk management (independent validation of model development, ongoing performance monitoring), ECOA adverse action notice generation (SHAP values for declined credit decisions — architecture must compute and store explanations). Architecture principle: compliance requirements drive architectural constraints — build the audit trail, explainability, and override mechanisms as first-class architectural features. Case study: Capital One's compliant AI architecture: automated compliance evidence collection from MLflow, CloudTrail, and GitHub — reduces SOC2 audit preparation from 8 weeks to 3 days." },
      { "title": "6.7 Module 6 Assessment: Enterprise AI Architecture Review", "desc": "Review a provided enterprise AI architecture for a retail bank's automated loan decisioning system. The architecture spans 3 legacy systems (mainframe credit bureau integration, 1990s risk scoring system, Oracle loan origination system), 2 cloud components (AWS SageMaker model, S3 data lake), and serves 50,000 daily decisions. Identify: integration anti-patterns, regulatory compliance gaps (ECOA adverse action notice capability, GDPR erasure mechanism, audit trail completeness), security gaps (DLP, third-party risk, IAM), and performance risks. Produce an architectural remediation plan prioritized by regulatory risk." }
    ]
  },
  {
    "title": "Module 7: Solution Design Process — From Business Problem to Deployed Architecture",
    "desc": "Great solutions architects produce architectures that get built and succeed in production. The solution design process, stakeholder communication, proof of concept design, cost estimation, and the solution delivery patterns used at AWS Professional Services, Accenture, and Deloitte.",
    "lessons": [
      { "title": "7.1 The Solution Design Process: Discovery, Design, Validate, Deliver", "desc": "Structured solution design prevents the most common architecture failure mode: building the right technical solution for the wrong problem. Discovery phase: stakeholder interviews (business owners, technical leads, operations teams — each has different requirements), current state documentation (existing systems, data flows, pain points), problem statement refinement ('we need AI' → 'we need to reduce customer churn from 15% to 10% using predictive intervention'), constraint identification (budget, timeline, regulatory, team skills). Design phase: generate 2-3 architectural options (each with different tradeoffs), evaluate against quality attributes, select and refine. Validate phase: proof of concept for the highest-risk assumptions (can the data support this model? can the latency be achieved?), stakeholder review, architecture review board. Deliver phase: detailed design documents, implementation roadmap, handoff to engineering team. Anti-pattern: skipping discovery (jumping straight to design based on the first stated requirements — solving the wrong problem expensively). Case study: McKinsey's AI strategy engagement framework follows the same four phases — discovery is where 40% of engagement time is spent because problem framing determines solution success." },
      { "title": "7.2 Technical Communication: Architecture Presentations for Different Audiences", "desc": "The same architecture must be communicated differently to engineers, executives, and external stakeholders. Executive presentation (C-suite): business problem → proposed solution (high level) → expected business outcomes (in business metrics, not technical metrics) → investment required → timeline. 5 slides maximum. No acronyms without explanation. Engineering presentation (engineering team): system context → component breakdown → data flows → key technical decisions and rationale → open questions. 15-30 slides. Technical depth expected and required. Stakeholder presentation (cross-functional: legal, compliance, product): problem statement → solution overview → specific impacts on their domain → compliance requirements addressed → approval request. The architecture decision communication: lead with the decision (not the analysis), explain the top 3 factors that drove the decision, acknowledge the main alternative and why it wasn't chosen, state what success looks like. Visual communication: use C4 diagrams for technical audiences, simplified block diagrams for executives, swim lane diagrams for process flows. Case study: how AWS Solution Architects structure their presentations — the 'working backwards' document (press release + FAQ) before any architecture work ensures the solution targets the right business outcome." },
      { "title": "7.3 Proof of Concept Design: Validating Assumptions Before Commitment", "desc": "A PoC validates the highest-risk assumptions before committing to a full architecture. PoC design principles: scope to the riskiest unknowns (not the familiar parts), time-box ruthlessly (2-4 weeks maximum), define success criteria before starting (what specific questions must the PoC answer?), use production-like data (synthetic data PoCs mislead). Common AI PoC questions: 'Can the available data support the required model accuracy?' (data quality and quantity validation), 'Can the latency requirement be met with the proposed architecture?' (end-to-end latency benchmark), 'Can the model detect the target pattern in production data?' (recall on labeled production samples). PoC vs. prototype vs. MVP: PoC (validates assumptions — throwaway code), prototype (demonstrates user experience — somewhat production-quality), MVP (minimal production system — must be maintainable). The PoC failure mode: PoCs that don't fail — a PoC that always succeeds wasn't testing risky enough assumptions. Case study: Klarna's AI customer service PoC: 2-week test on 2% of production traffic, measured CSAT and resolution rate vs. human agents — PoC provided the business case for full deployment and identified required accuracy thresholds." },
      { "title": "7.4 Architecture Cost Estimation: Cloud Resource Sizing and TCO Models", "desc": "Architects are responsible for the financial feasibility of their designs. Bottom-up cloud cost estimation: identify every infrastructure component (compute instances, storage, network, managed services), size each component (requests/second → required CPU/memory, data volume → storage GB), apply pricing (AWS/GCP/Azure pricing calculator), multiply by utilization and growth. TCO model components: infrastructure (cloud bill), engineering (implementation and maintenance labor), operations (monitoring, on-call), licensing (software, data), compliance (audit, regulatory). Cost modeling tools: AWS Pricing Calculator, GCP Pricing Calculator, Infracost (Terraform-based cost estimation in CI/CD). Common estimation mistakes: ignoring data egress costs (can be 30% of total bill), forgetting managed service premiums (RDS vs. EC2+PostgreSQL), not modeling usage growth (costs typically 3x in year 2). The cost architecture review: for any proposed architecture, produce a 3-year TCO model with sensitivity analysis (best case, expected, worst case). The cost optimization loop: estimate → build → measure actual costs → optimize → re-estimate. Case study: the TCO analysis that convinced Dropbox to move from AWS to their own data centers — their specific workload profile made on-premises cheaper at their scale." },
      { "title": "7.5 Scalability Architecture: Designing for 100x Growth", "desc": "Architects must design systems that can scale beyond current requirements without full redesign. Scalability bottleneck identification: every component has a scaling limit. Stateless services (scale horizontally — add replicas), stateful services (scale with partitioning — Kafka partition count, Redis sharding), databases (scale with read replicas for reads, sharding for writes), external API dependencies (rate limits — may require caching or queuing). The scalability ceiling of common components: single PostgreSQL instance (~10K writes/second), single Redis instance (~100K ops/second), single Kafka partition (~1M messages/second). Designing past the ceiling: partition PostgreSQL by tenant (each tenant has their own database), shard Redis horizontally (Redis Cluster), increase Kafka partition count (requires consumer rebalancing). The read/write scaling mismatch: most AI systems read far more than they write (feature serving: 1M reads/second, materialization: 1K writes/second) — use read replicas and caching to serve reads, primary database only for writes. Case study: Slack's messaging architecture scaled from 1M to 20M daily active users — their architecture blog post documents the specific scaling decisions made at each inflection point." },
      { "title": "7.6 Architecture Risk Management: Identifying and Mitigating Technical Risk", "desc": "Every architecture has risks — the architect's job is to identify them explicitly and design mitigations. Risk taxonomy for AI architectures: data risk (training data quality, availability, and freshness — missing data kills models), model risk (accuracy degradation, bias, adversarial robustness — models fail in unexpected ways), infrastructure risk (availability, performance under load, disaster recovery — infrastructure fails at the worst times), integration risk (third-party dependencies, API stability, schema evolution — external dependencies break), regulatory risk (compliance changes, audit findings, enforcement actions — regulations evolve). Risk assessment methodology: probability × impact matrix, with mitigation actions for high-risk items. Risk mitigation patterns: data risk → data quality monitoring, fallback data sources. Model risk → continuous monitoring, canary deployments. Infrastructure risk → chaos engineering, SLO-based alerting. Integration risk → contract testing, API versioning. Regulatory risk → compliance automation, proactive audit preparation. The architecture risk register: living document tracking identified risks, probability, impact, mitigation status, and owner. Case study: Google's Site Reliability Engineering practice applies risk management to all infrastructure — their 'risk analysis' section of design docs is mandatory for high-risk architectural changes." },
      { "title": "7.7 Module 7 Capstone: Full Solution Design Engagement", "desc": "Simulate a complete solution design engagement: discovery (analyze provided stakeholder interview transcripts and existing system documentation), design (produce 2 architectural options with tradeoffs), validate (design PoC for the riskiest assumption), estimate (3-year TCO model for recommended option), communicate (prepare 3 presentations — executive summary, engineering deep dive, compliance review). Recommended architecture must address: functional requirements, quality attributes, regulatory constraints, cost targets, and organizational change management." }
    ]
  },
  {
    "title": "Module 8: Emerging AI Architecture Patterns — Agents, RAG at Scale, and Foundation Models",
    "desc": "AI architecture is evolving rapidly. Agentic AI architecture, production RAG system design, foundation model serving, multi-agent coordination, and the architectural patterns enabling the next generation of AI applications.",
    "lessons": [
      { "title": "8.1 Agentic AI Architecture: Designing for Autonomous Action", "desc": "AI agents that take real-world actions require architectural patterns unlike passive prediction systems. Agent architecture components: perception layer (what information the agent can observe — tools, memory, context), reasoning layer (the LLM that plans and decides — prompt engineering, chain-of-thought), action layer (tools the agent can use — web search, code execution, database queries, API calls), memory layer (short-term context window, long-term vector store). The agent loop: observe → plan → act → observe → (repeat). Safety architecture for agents: permission scoping (agents can only use tools explicitly granted), rate limiting (maximum N actions per minute prevents runaway agents), confirmation gates (irreversible actions require explicit human confirmation), audit trail (every action logged with the triggering reasoning). Human-in-the-loop architecture: when and how humans intervene in agent execution. The capability-risk tradeoff: more capable agents (more tools, more autonomy) = higher risk (more potential for unintended consequences). Case study: Anthropic's Computer Use capability requires careful permission scoping — the agent can browse the web and interact with applications, but the architecture isolates it from production systems and financial actions." },
      { "title": "8.2 Production RAG Architecture: Retrieval, Indexing, and Quality at Scale", "desc": "Production RAG systems serving millions of queries require architecture beyond the tutorial example. Indexing architecture: document processing pipeline (PDF/HTML parsing → chunking strategy → embedding model → vector store). Chunking decisions: fixed-size chunks (simple, misses semantic boundaries), semantic chunks (sentence transformers to detect boundaries), hierarchical chunks (parent document + child chunks — retrieve child, return parent). Multi-vector indexing: index the same document with multiple representations (dense embedding for semantic search, BM25 index for keyword search, summary embedding for high-level retrieval) — hybrid search combines all three. Retrieval quality at scale: approximate nearest neighbor search (HNSW algorithm — 95%+ recall at millisecond latency vs. exact search), reranking pipeline (retrieve top-50 candidates, cross-encoder reranks to top-5), query rewriting (LLM rewrites unclear queries before retrieval). Freshness architecture: incremental indexing (add new documents without rebuilding entire index), deletion handling (vector stores must support deletion for GDPR compliance). Case study: Notion AI indexes 100M+ user documents — their chunking strategy and incremental indexing architecture is required to serve personalized retrieval at this scale." },
      { "title": "8.3 Vector Database Architecture: Selection, Scaling, and Production Operations", "desc": "Vector database selection is the most impactful infrastructure decision in any RAG system. Vector database comparison: Pinecone (managed, simple operations, high cost at scale), Weaviate (hybrid search native, rich filtering API, self-hosted or managed), Qdrant (Rust implementation, best throughput/cost ratio for self-hosted, gRPC API), pgvector (zero new infrastructure for Postgres shops, limited at >10M vectors), Milvus (highest throughput for massive scale, complex operations). Selection criteria: vector count (pgvector: <1M, Weaviate/Qdrant: <100M, Milvus: >100M), query complexity (rich metadata filtering → Weaviate, simple search → any), operational preference (managed → Pinecone, self-hosted → Qdrant), budget (Pinecone expensive at scale, Qdrant/pgvector much cheaper). Production operations: index backup (export vectors and metadata for DR), monitoring (query latency P99, recall rate, memory utilization), capacity planning (memory required = vector count × vector dimensions × bytes_per_float). Case study: Slack's AI search uses pgvector for their enterprise search feature — their message volume (200B+ messages) required careful sharding strategy to scale pgvector beyond its default limits." },
      { "title": "8.4 Foundation Model Architecture: Serving, Fine-Tuning, and Model Selection", "desc": "Foundation models (GPT-4, Claude, Llama, Gemini) are now architectural components, not just tools. Make vs. buy vs. fine-tune decision: API (fastest to deploy, highest per-token cost, external data risk), open-source self-hosted (moderate cost, moderate operational burden, no data privacy concern), fine-tuned open-source (best accuracy for domain, highest operational cost, requires training infrastructure). Fine-tuning architecture: instruction dataset curation (quality >> quantity — 1,000 curated examples > 100,000 noisy), LoRA (Low-Rank Adaptation — train 0.1% of parameters, retain base model knowledge), evaluation pipeline (capability regression testing — fine-tuned model must not lose general capability). Foundation model serving architecture: vLLM (PagedAttention — 24x concurrent users vs. naive serving), TensorRT-LLM (NVIDIA-optimized serving — 2-4x throughput), speculative decoding (draft model proposes tokens, large model verifies — 2-3x throughput). Orchestration: LangChain/LlamaIndex for RAG pipelines, LangGraph for agentic workflows, direct API calls for simple inference. Case study: Mistral AI's architecture choices: open-source Mistral-7B is used by 50,000+ companies — their serving architecture handles diverse workloads from RAG to chat to classification through a single model endpoint with appropriate system prompts." },
      { "title": "8.5 Multi-Agent System Architecture: Coordination, Communication, and Failure Handling", "desc": "Multi-agent systems coordinate multiple AI agents to accomplish complex tasks. Coordination architectures: orchestrator-worker (a central orchestrator agent assigns tasks to specialized worker agents — clear control flow, single point of failure), peer-to-peer (agents communicate directly — more resilient, harder to reason about), hierarchical (agents organized in layers — scalable for complex tasks, complex to debug). Inter-agent communication: tool calls (agent A calls agent B's API as a tool), message passing (agents publish/subscribe to message queues), shared state (agents read/write shared memory store). Failure handling in multi-agent systems: agent timeout (orchestrator retries with different worker or falls back to simpler approach), task decomposition failure (agent cannot complete assigned subtask — escalate to human or orchestrator replans), infinite loop detection (detect when agents are cycling — terminate after N iterations). Observability for multi-agent: distributed tracing across agent boundaries (trace_id propagated through all agent calls — visualize complete task execution in Jaeger/Langfuse). Trust between agents: an agent receiving instructions from another agent should not have more trust than a user input — prompt injection through agent messages is a real attack vector. Case study: AutoGen (Microsoft) multi-agent framework: code writing agent, code review agent, test execution agent coordinate to implement software features — the architecture handles failure through orchestrator-level retry logic." },
      { "title": "8.6 AI System Observability: Monitoring LLMs, Agents, and RAG Pipelines", "desc": "AI system observability requires new metrics beyond traditional infrastructure monitoring. LLM observability metrics: token consumption per request (cost proxy), latency to first token (user experience indicator), completion token ratio (are responses appropriately long?), prompt/completion ratio (optimization target). RAG observability: retrieval precision (are retrieved chunks relevant?), answer faithfulness (is the answer grounded in retrieved context?), retrieval latency (P99 for vector search). Agent observability: tool call frequency by tool (which tools are used most?), task completion rate (what percentage of agent tasks succeed?), action count per task (are agents efficient?), human escalation rate (how often do agents need human intervention?). Evaluation pipeline: LLM-as-judge for periodic quality assessment, golden dataset regression (100 curated input-output pairs run weekly), drift detection (output distribution shift over time). Platforms: Langfuse (open-source LLM observability), Weights & Biases Prompts (experiment tracking for LLM), Braintrust (evaluation + observability), Helicone (LLM proxy with analytics). Case study: Notion AI observability: they track 15 metrics per RAG request — the combination of retrieval precision, generation faithfulness, and latency has guided 3 major architecture improvements over 18 months." },
      { "title": "8.7 Module 8 Assessment: Emerging AI Architecture Design", "desc": "Design the architecture for an enterprise AI assistant that can: answer questions from internal documents (RAG), execute multi-step workflows by coordinating specialized agents (code generation, data analysis, email drafting), and take actions in enterprise systems (create Jira tickets, update Salesforce records, send Slack messages). Address: RAG indexing pipeline for 5M internal documents, multi-agent coordination architecture, permission scoping for each enterprise system integration, safety controls (confirmation gates, rate limits, audit trail), observability stack, and failure handling. Include threat model for prompt injection and data exfiltration." }
    ]
  },
  {
    "title": "Module 9: AI Architecture for Specific Domains — Healthcare, Finance, Retail, and Manufacturing",
    "desc": "Domain-specific AI architecture requires understanding the regulatory constraints, data characteristics, and business processes unique to each industry. Healthcare AI under HIPAA and FDA, financial services under SR 11-7 and GDPR, retail recommendation at scale, and manufacturing predictive maintenance.",
    "lessons": [
      { "title": "9.1 Healthcare AI Architecture: HIPAA, FDA, and Clinical Decision Support", "desc": "Healthcare AI architecture must satisfy HIPAA technical safeguards while meeting clinical performance requirements. HIPAA technical safeguards: access control (unique user identification, automatic logoff, encryption), audit controls (hardware, software, and procedural mechanisms to record and examine access), integrity controls (mechanisms to authenticate ePHI has not been altered), transmission security (encryption for data in transit). Data architecture for HIPAA: de-identification (Safe Harbor method: remove 18 specific identifiers; Expert Determination: statistical analysis proving re-identification risk < 0.01%), limited data sets (remove direct identifiers, data use agreement required). FDA clinical decision support classification: non-device CDS (software that displays, analyzes, or presents information to support clinical decision-making, where the clinician independently reviews the basis — not regulated), device CDS (software that analyzes patient-specific information to provide diagnosis or treatment recommendations without clinician independent review — regulated as SaMD). Architecture choice: design clinical AI as non-device CDS where possible (display model output alongside evidence, clinician makes final decision) — avoids 12-18 month FDA clearance process. Case study: Google's DeepMind diabetic retinopathy AI: originally classified as SaMD (requires FDA clearance), redesigned as non-device CDS (displays retinal analysis to ophthalmologist who makes final diagnosis) — enabling faster deployment while maintaining clinical value." },
      { "title": "9.2 Financial Services AI Architecture: Model Risk, Explainability, and Compliance", "desc": "Financial services AI architecture is the most heavily regulated domain. SR 11-7 model risk management architecture: model inventory (every ML model registered with risk rating), model validation (independent validation of development methodology, data, and testing — separate team from model development), ongoing monitoring (performance metrics tracked, threshold breaches trigger revalidation), model governance (change management process for model updates). Credit scoring architecture requirements: ECOA adverse action notices (SHAP values computed for every declined decision, stored for 25 months), Reg B compliance documentation, Fair Housing Act compliance for mortgage AI. Anti-money laundering (AML) architecture: graph ML for transaction network analysis (identify suspicious patterns), case management system integration (ML generates alerts, human investigators review), SAR (Suspicious Activity Report) generation automation. Fraud detection architecture: real-time scoring (<200ms), feature store with sub-second freshness (transaction velocity features), fallback to rule-based decisions if ML unavailable (regulatory requirement: systems affecting customers must have defined fallback behavior). Case study: JP Morgan's COIN (Contract Intelligence) system: document intelligence AI processes 12,000 commercial credit agreements in seconds vs. 360,000 lawyer-hours manually — the architecture includes complete audit trail required for regulatory examination." },
      { "title": "9.3 Retail and E-Commerce AI Architecture: Recommendations at Scale", "desc": "Retail AI architecture must handle extreme scale (Black Friday: 10x normal traffic) and tight latency budgets (recommendations must not slow page loads). Recommendation system architecture: candidate generation (fast retrieval — ANN search over item embeddings, reduces 10M items to 1,000 candidates in <10ms), scoring (ML model scores 1,000 candidates in <50ms), filtering (business rules remove out-of-stock, age-restricted, geographically unavailable items), ranking (final ML ranking optimizes for conversion × margin), blending (mix personalized with trending/diverse items for discovery). Cold start architecture: new users → popularity-based recommendations, context-based (session behavior), or demographic-based. New items → content-based (item attributes), trending signals, seller-promoted (explicit boost). Feature freshness: click and purchase signals require <5 minute freshness (user behavior changes rapidly), item features (price, inventory) require real-time freshness (OOS items must be removed immediately). Peak traffic architecture: pre-compute recommendations for high-traffic users (top 10M users precomputed nightly), serve from cache during peak. Case study: Amazon's recommendation engine: responsible for 35% of revenue — their 'customers who bought X also bought Y' architecture runs 500M+ daily prediction requests using candidate generation + neural scoring pipeline." },
      { "title": "9.4 Manufacturing AI Architecture: Predictive Maintenance and Quality Control", "desc": "Manufacturing AI operates in OT (Operational Technology) environments with unique constraints: air-gapped networks, legacy PLC/SCADA systems, safety-critical requirements, and real-time processing of high-frequency sensor data. Data collection architecture: IoT sensors (temperature, vibration, pressure, current) → edge computing (Raspberry Pi or industrial edge device runs preprocessing — reduces 100K samples/second to 10 feature values/second), edge → cloud batch upload (compressed feature data uploaded hourly) or streaming (MQTT → Kafka for real-time monitoring). Predictive maintenance architecture: anomaly detection (autoencoder trained on normal operation, reconstruction error > threshold triggers alert), remaining useful life (RUL) prediction (LSTM trained on historical failure patterns), alert routing (maintenance management system integration). Quality control architecture: computer vision inspection (camera → edge inference → pass/fail → conveyor control signal), sub-100ms latency required for manufacturing line speed. Air-gapped deployment: models trained in cloud, deployed to edge devices via one-way data transfer (USB, secure file drop) — no internet access from production floor. Case study: Siemens' MindSphere predictive maintenance platform: 300,000+ connected devices, $1B+ in avoided unplanned downtime annually — their edge-to-cloud architecture handles the OT/IT boundary." },
      { "title": "9.5 Natural Language Processing Architecture: Text Processing at Enterprise Scale", "desc": "NLP architecture for enterprise use cases spans document processing, search, and conversational AI. Document processing pipeline: OCR (Tesseract or AWS Textract for scanned documents), layout parsing (detect tables, headers, sections — LayoutLM, Unstructured.io), entity extraction (spaCy NER, AWS Comprehend, or fine-tuned BERT), classification (document type, routing decision), storage (original document + extracted structured data + embeddings). Enterprise search architecture: hybrid search (BM25 for keyword + dense retrieval for semantic — Elasticsearch + vector store), query understanding (intent classification, entity extraction, query expansion), results re-ranking (cross-encoder or LLM-based). Conversational AI architecture: intent classification → entity extraction → dialog management (state machine or LLM-based) → response generation (template-based or LLM) → response validation (safety classifier). Multi-language architecture: language detection → routing to language-specific models or multilingual models (mBERT, XLM-RoBERTa) → translation for cross-language queries. Case study: Salesforce's Einstein NLP architecture: processes 100B+ text events daily for sentiment analysis, entity extraction, and classification across all Salesforce products — their NLP platform is a shared service consumed by 50+ Salesforce features." },
      { "title": "9.6 Computer Vision Architecture: Image and Video Processing at Scale", "desc": "Computer vision architecture must handle the unique data characteristics of images and video (large file sizes, high-dimensional embeddings, real-time requirements for video). Image processing pipeline: ingestion (S3 + event trigger on upload), preprocessing (resize, normalize, augment for training), embedding generation (ResNet/ViT feature extraction — stored for retrieval), model inference (classification, detection, segmentation). Video processing architecture: frame extraction (FFmpeg at 1-10 FPS for most use cases), keyframe selection (perceptual hashing to remove near-duplicate frames), parallel frame processing (Kubernetes jobs fan out across frames), temporal aggregation (combine frame-level features into video-level prediction). Edge inference for computer vision: TensorFlow Lite or ONNX Runtime on edge hardware, INT8 quantization (4x size reduction, 2x speed increase), batch frames when possible (single GPU pass for 16 frames vs. 16 separate passes). Large-scale visual search: FAISS (Facebook AI Similarity Search — billion-scale ANN search for images), approximate search (HNSW or IVF index — >95% recall at millisecond latency). Case study: Pinterest's visual search architecture: index 10B+ images, 100M+ daily visual searches — their multi-scale embedding approach enables search across different levels of visual abstraction." },
      { "title": "9.7 Module 9 Assessment: Domain Architecture Case Study", "desc": "Select one domain (healthcare, financial services, retail, or manufacturing) and design the complete AI architecture for a specific use case: patient readmission risk (healthcare), algorithmic credit scoring (financial services), personalized product recommendations (retail), or predictive equipment maintenance (manufacturing). The design must address: data architecture with domain-specific constraints, ML pipeline with appropriate validation, serving architecture with latency requirements, compliance controls specific to the domain, security architecture, and failure handling. Produce system diagram and written architecture document." }
    ]
  },
  {
    "title": "Module 10: Architecture Leadership — Communication, Strategy, and the Solutions Architect Career",
    "desc": "Senior architects shape technical direction across organizations. Architectural governance, technical strategy development, building architectural influence, communicating with executives and external stakeholders, and your career path from architect to CTO.",
    "lessons": [
      { "title": "10.1 Architectural Governance: Standards, Principles, and Technical Debt Management", "desc": "Architectural governance ensures that individual decisions accumulate into a coherent system rather than a collection of incompatible components. Architecture principles: documented, organization-wide technical decisions (prefer managed cloud services for undifferentiated infrastructure, use open standards to avoid vendor lock-in, design for horizontal scalability from the start). Technology radar: the Thoughtworks Technology Radar model — quarterly assessment of technologies across four quadrants (Adopt, Trial, Assess, Hold). Technical debt register: systematic tracking of architectural shortcuts with estimated remediation cost and business risk. The architecture review board: senior architects review high-impact decisions that affect multiple teams or create long-term constraints — not every decision, only decisions that cross boundaries. Architecture governance tools: Architecture Decision Records (ADRs) in a central repository, architecture diagram repository (C4 diagrams versioned with code), architectural fitness functions (automated tests that verify architectural constraints are maintained). Case study: Spotify's architectural governance: the 'Opinionated Spotify Architecture' document defines technology choices (Kafka for messaging, Kubernetes for compute, Backstage for IDP) that all teams follow — enabling interoperability without centralizing all decisions." },
      { "title": "10.2 Technical Strategy: 3-Year Architecture Roadmap Development", "desc": "Senior architects develop multi-year technical strategies that align with business direction. Strategy development process: assess current state (capability map, technical debt inventory, team skill gaps), understand business direction (roadmap review with product and executive leadership), identify strategic bets (what technical investments will create the most business leverage?), define architectural north star (what does the ideal state look like in 3 years?), sequence the roadmap (what must be done first to enable subsequent investments?). The technology lifecycle management: some technologies are strategic investments (build capability), some are in maintenance mode (sustain and reduce dependency), some are being sunset (migrate away). Roadmap communication: 1-page strategy poster (visual overview of direction and key initiatives), 12-month detailed roadmap (quarterly initiatives with dependencies), 3-year aspiration (north star vision). The architect's political challenge: technical strategy requires organizational alignment — engineers must believe in it, executives must fund it, product teams must cooperate with it. Case study: AWS's re:Invent keynotes are public 3-year technical strategy announcements — each year's keynote shows how investments from 3 years ago are paying off and what investments are being made now." },
      { "title": "10.3 Building Architectural Influence Without Authority", "desc": "Solutions architects typically influence without direct authority — the architect recommends, teams decide. Influence strategies for architects: credibility (deep technical knowledge demonstrated through accurate, insightful analysis — trust takes months to build, seconds to lose), relationship-building (architects who participate in design from the start have far more influence than those who review at the end), making the right choice easy (provide tools, templates, and reference architectures that make the recommended approach simpler than alternatives), the pull vs. push dynamic (sharing knowledge freely creates pull — engineers come to the architect for advice vs. architect pushing recommendations onto teams). The architectural influence toolkit: reference architectures (canonical implementations of common patterns), decision frameworks (tools that help teams make the right tradeoff for their context), office hours (weekly sessions where teams can get architectural guidance), architecture reviews (structured forums where architects provide feedback). The architect's influence ceiling: architects who can only influence through formal authority are limited; architects who build genuine credibility and relationships can shape organizations. Case study: Martin Fowler's influence on software architecture globally: books, blog posts, and conference talks create influence far beyond any organizational authority." },
      { "title": "10.4 Communicating Architecture to Executives: Business Value Translation", "desc": "Technical excellence is invisible to executives — it must be translated into business terms. The executive communication framework for architects: start with the business outcome (not the technology), quantify the impact (revenue, cost, risk, speed — in dollars and percentages), explain the constraint that architecture addresses (and what happens if not addressed), state the investment required, and define measurable success criteria. Common architectural investments and their business translation: unified data platform (from: 6 weeks to produce new ML features, to: 2 weeks with shared feature store — 3x faster AI product development), Kubernetes migration (from: 4-hour deployment lead time, to: 15 minutes — 16x faster time to market), observability platform (from: 4-hour mean time to resolution, to: 30 minutes — 87% reduction in customer impact duration). The executive's questions: why now? (what's the cost of waiting?), why this? (why not a simpler/cheaper solution?), what could go wrong? (architect must address risks proactively, not wait to be asked). Case study: how Netflix's Reed Hastings communicates about infrastructure investment: he speaks about engineering velocity and competitive positioning — never about Kubernetes or microservices. Architectural investments are discussed in terms of business capabilities they enable." },
      { "title": "10.5 External-Facing Architecture: Customer Solutions and Technical Sales", "desc": "Customer-facing solutions architects translate customer business problems into technical solutions using the vendor's platform. The customer engagement lifecycle: discovery (understand customer's business problems and current technical environment — ask more than you tell), solution design (map customer requirements to platform capabilities — be honest about gaps), proof of concept support (guide customer through PoC that validates the proposed solution), implementation support (review customer's implementation architecture — catch design issues before they reach production), success measurement (track customer outcome metrics, not just deployment metrics). Technical sales support: RFP responses (address technical requirements with specific architecture evidence), competitive differentiation (explain why your platform's architecture is better for this use case), architectural risk assessment (identify risks in customer's current architecture that the platform addresses). Case study: AWS Solutions Architects are the highest-leverage sales resource — customers often credit their SA with the decision to move to AWS. Their technical credibility and ability to solve real problems creates more trust than any marketing material." },
      { "title": "10.6 The Solutions Architect Career Path: From SA to CTO", "desc": "Solutions architect is one of the highest-leverage careers in technology — and one of the most misunderstood. Career track options: technical (SA → Senior SA → Principal SA → Distinguished SA/Fellow — deep technical authority, organization-wide influence), management (SA → Engineering Manager → Director of Architecture → VP Engineering/CTO — people management + technical strategy), consulting (SA → Senior Consultant → Partner — customer-facing, high variation, variable income). Compensation benchmarks (2024, US): SA L3 $130-170K, Senior SA L4 $170-230K, Principal SA L5 $230-310K, Distinguished SA/Fellow $350K+, CTO at startup $250-500K+. Skills that differentiate senior architects: systems thinking (reason about emergent behavior across complex systems), communication (make technical complexity accessible to all audiences), judgment (know when to be flexible vs. when to hold firm on architectural principles), strategic vision (connect current technical decisions to long-term business outcomes). Portfolio for architects: public architectural work (conference talks, blog posts, open-source contributions), case studies (documented successful projects with measurable outcomes), certifications (AWS Solutions Architect Professional, GCP Professional Cloud Architect, TOGAF). Case study: the career trajectory of Werner Vogels (CTO Amazon): researcher → systems architect → VP/CTO — the breadth of systems thinking applied to Amazon's architecture at scale." },
      { "title": "10.7 Final Capstone: Full Architecture Engagement", "desc": "Simulate a complete end-to-end architecture engagement for a Series C AI company. Provided materials: executive brief, stakeholder interview transcripts, existing system documentation, budget constraints, and regulatory requirements. Deliverables: discovery synthesis (key insights from stakeholder analysis), 3 architectural options with tradeoffs, recommended architecture with complete documentation (C4 context and container diagrams, ADRs for 5 key decisions, cost estimate, risk register), PoC design for highest-risk assumption, executive presentation (5 slides: problem → solution → outcomes → investment → timeline), and engineering deep-dive presentation (15 slides). Assessment criteria: quality of problem framing, completeness of tradeoff analysis, realism of cost estimate, clarity of communication across audiences." }
    ]
  }
]
};

serve(async (req) => {
  if (req.method === "OPTIONS") return new Response(null, { headers: corsHeaders });
    const _denied = await requireAdmin(req);
    if (_denied) return _denied;
  try {
    const supabase = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
    const COURSE_TITLE = courseData.title;
    const { data: ex } = await supabase.from("courses").select("id").eq("title", COURSE_TITLE).maybeSingle();
    if (ex) await supabase.from("courses").delete().eq("id", ex.id);
    const { data: course, error: ce } = await supabase.from("courses").insert({ title: courseData.title, description: courseData.description, is_published: true, translations: courseData.translations }).select().single();
    if (ce) throw ce;
    let chaptersCreated = 0; let videosCreated = 0;
    for (let mi = 0; mi < courseData.modules.length; mi++) {
      const mod = courseData.modules[mi];
      const { data: chapter, error: che } = await supabase.from("chapters").insert({ course_id: course.id, title: mod.title, description: mod.desc, order_index: mi, translations: {} }).select().single();
      if (che) throw che; chaptersCreated++;
      for (let li = 0; li < mod.lessons.length; li++) {
        const les = mod.lessons[li];
        const transcript = les.title + "\n\n" + les.desc + "\n\nThis is a world-class lesson from Aladiah Academy's professional certification program. Prof. Didier teaches this with production depth, real case studies, and implementation guidance. Module context: " + mod.title + ". " + mod.desc;
        const trans = { en:{title:les.title,transcript}, es:{title:les.title,transcript}, fr:{title:les.title,transcript}, de:{title:les.title,transcript}, zh:{title:les.title,transcript}, ar:{title:les.title,transcript}, ja:{title:les.title,transcript} };
        const { data: video, error: ve } = await supabase.from("videos").insert({ chapter_id: chapter.id, title: les.title, description: les.desc, order_index: li, translations: trans }).select().single();
        if (ve) throw ve; videosCreated++;
        const { data: quiz, error: qe } = await supabase.from("quizzes").insert({ video_id: video.id, chapter_id: chapter.id, quiz_type: "mini_video", passing_score: 85 }).select().single();
        if (qe) throw qe;
        await supabase.from("quiz_questions").insert([
          { quiz_id: quiz.id, question_text: "What production expertise does this lesson build?", scenario_context: "Applying this in a professional role.", options: [les.desc.substring(0,120)+"...", "Only theoretical background", "Covered elsewhere", "Optional content"], correct_answer_index: 0, explanation: les.desc, order_index: 0 },
          { quiz_id: quiz.id, question_text: "Which real-world organizations' practices are referenced?", scenario_context: "Grounding knowledge in production reality.", options: ["Leading global organizations — case studies ground every lesson in production reality", "No real organizations referenced", "Only academic examples", "Only startups"], correct_answer_index: 0, explanation: "Every lesson uses production case studies from world-class organizations.", order_index: 1 },
        ]);
      }
      const { data: eq, error: eqe } = await supabase.from("quizzes").insert({ chapter_id: chapter.id, quiz_type: "chapter_end", passing_score: 85 }).select().single();
      if (eqe) throw eqe;
      await supabase.from("quiz_questions").insert([
        { quiz_id: eq.id, question_text: "What expert-level competency does this module develop?", scenario_context: "Module completion assessment.", options: [mod.desc.substring(0,140)+"...", "Only introductory familiarity", "Theoretical knowledge only", "Not career-relevant"], correct_answer_index: 0, explanation: mod.desc, order_index: 0 },
        { quiz_id: eq.id, question_text: "What career level does mastery of this module support?", scenario_context: "Professional development planning.", options: ["Senior professional and leadership roles in the fastest-growing AI specialization globally", "Entry-level only", "Academic roles only", "No direct career value"], correct_answer_index: 0, explanation: "Each module builds expertise directly valued in high-compensation AI professional roles.", order_index: 1 },
      ]);
    }
    return new Response(JSON.stringify({ message: courseData.title + " — 10-module world-class version seeded", courseId: course.id, modules: chaptersCreated, lessons: videosCreated }), { headers: { ...corsHeaders, "Content-Type": "application/json" } });
  } catch (e: unknown) {
    return new Response(JSON.stringify({ error: e instanceof Error ? e.message : String(e) }), { status: 500, headers: { ...corsHeaders, "Content-Type": "application/json" } });
  }
});
