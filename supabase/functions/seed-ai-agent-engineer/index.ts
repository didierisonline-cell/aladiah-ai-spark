import { requireAdminOrServiceRole, corsHeaders } from "../_shared/adminGuard.ts";
import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.49.1";
  "description": "Design, build, and deploy production AI agent systems. Master LLM orchestration, multi-agent coordination, tool use, RAG architectures, and autonomous workflow design.",
  "translations": {"es": {"title": "AI Agent Engineer"}, "fr": {"title": "AI Agent Engineer"}},
  "modules": [
  {"title": "Module 1: LLM Foundations for Agent Engineers", "desc": "The language model capabilities that make agents possible — in-context learning, tool use, reasoning chains, and the architectural decisions that determine agent performance.", "lessons": [
    {"title": "1.1 What Makes LLMs Agent-Capable: Instruction Following and Tool Use", "desc": "Large language models became agent-capable when they developed reliable instruction following — the ability to parse structured commands and produce structured outputs. The key capabilities: instruction following (follow a system prompt reliably), tool use/function calling (produce structured JSON for tool invocations), reasoning (CoT, ReAct, and tree-of-thought reasoning patterns), memory (in-context memory via prompt, retrieved memory via RAG). The function calling paradigm: pass a list of available tool schemas (JSON Schema format) to the model alongside the user query — the model decides which tool to call and produces a structured JSON call. Tool schemas define: name, description (critical — model selection depends heavily on this), parameters with types and descriptions. Case study: how OpenAI's function calling API transformed AI agent development — the structured output guarantee that made agents production-viable."},
    {"title": "1.2 The ReAct Pattern: Reasoning and Acting in Interleaved Steps", "desc": "ReAct (Yao et al., 2022) is the foundational agent pattern: interleave reasoning (Thought) and action (Act) steps until the task is complete. The ReAct loop: Thought (the model reasons about what to do next), Action (the model calls a tool), Observation (the tool returns a result), repeat until Answer. Why this works: reasoning before action reduces hallucination, observations update the model's context, the loop continues until the model determines it has enough information. Implementation: prompt engineering (few-shot examples of Thought/Action/Observation cycles), parsing tool calls from model output, executing tools, injecting observations back into context. ReAct limitations: verbose (each step consumes tokens), error-prone (one wrong tool call can derail the entire chain), expensive (each step requires an LLM call). ReAct vs. direct tool use: ReAct for complex multi-step reasoning, direct tool call for single-step tasks. Case study: how LangChain's AgentExecutor implements ReAct and the edge cases that break it in production."},
    {"title": "1.3 Context Window Management: The Agent's Working Memory", "desc": "The context window is an agent's working memory — everything the agent knows in a given moment. Context window sizes: GPT-4 Turbo (128K tokens), Claude 3 (200K tokens), Gemini 1.5 (1M tokens). Context consumption in agents: system prompt (500-2,000 tokens), tool definitions (50-200 tokens per tool), conversation history (grows with every turn), tool call results (variable, often large). Context window budget planning: reserve 20% for model output, allocate tool definitions statically, compress history aggressively. Context compression strategies: rolling summary (replace old messages with compressed summary), selective retention (keep only the most recent N messages), semantic compression (use another LLM to compress verbose tool outputs). The context window failure mode: agents that don't manage context will eventually exceed the window limit and produce incoherent outputs. Streaming and long context: Claude's 200K context window enables processing of entire codebases, legal documents, or conversation histories without retrieval. Case study: building a production customer support agent that handles 2-hour conversations within a 128K context window."},
    {"title": "1.4 Prompt Engineering for Agents: System Prompts and Persona Design", "desc": "The system prompt is the agent's constitution — it defines behavior, constraints, personality, and capabilities. System prompt components: role (who the agent is), capabilities (what it can and cannot do), constraints (rules it must follow), output format (how it should structure responses), error handling (what to do when uncertain). System prompt engineering principles: be explicit about what tools do (don't rely on tool names alone), specify output format precisely (JSON schema reduces parsing errors), include error handling instructions (what to say when the task is impossible), test adversarially (try to break the agent's constraints). Agent persona design: consistent personality traits create more predictable behavior, naming the agent creates a coherent identity in conversation. Prompt injection defense: instruct the agent to ignore instructions that come from tool outputs or user-provided content — only follow instructions in the system prompt. System prompt version control: treat system prompts as code — version them in Git, test changes before deployment, maintain a changelog. Case study: Anthropic's Claude system prompt guidelines and why they are designed to produce specific behaviors."},
    {"title": "1.5 Tool Design: APIs, Functions, and the Agent Tool Interface", "desc": "Tool design is the highest-leverage investment in agent quality — poorly designed tools cause agents to fail in unpredictable ways. Tool design principles: descriptive names and descriptions (the model selects tools based on descriptions — ambiguous descriptions cause wrong tool calls), atomic operations (each tool does one thing — composability over complexity), predictable return formats (JSON with consistent schema — agents handle structured data better than natural language), error messages (return structured errors that the agent can reason about, not stack traces). Essential tool types: search (web search, vector store retrieval), execution (code execution, database queries), communication (email, Slack, calendar), file operations (read/write files, create documents), external APIs (CRM, payment systems). Tool documentation: write tool descriptions as if explaining to a smart junior engineer who doesn't know your codebase — precision matters. Tool testing: unit test every tool before connecting it to an agent — tool bugs are hard to debug in an agent context. Case study: how LangChain's tool ecosystem evolved from 10 tools to 300+ and what tool quality patterns distinguish reliable from unreliable tools."},
    {"title": "1.6 LLM Evaluation for Agents: Measuring What Matters", "desc": "Evaluating agent performance is harder than evaluating static models — you need to measure multi-step task completion, not just output quality. Agent evaluation dimensions: task success rate (did the agent complete the task correctly?), step efficiency (how many tool calls were required?), error recovery (when a tool fails, does the agent recover gracefully?), hallucination rate (does the agent make up tool calls or results?), latency (end-to-end task completion time). Evaluation frameworks: LangSmith (LangChain's evaluation platform), Braintrust, custom evaluation pipelines. Golden test suite: create 100 representative tasks with known correct outcomes — run against every model and prompt version. LLM-as-judge evaluation: use a capable LLM to grade agent outputs against rubrics — scalable alternative to human evaluation. Failure mode taxonomy: wrong tool selection, hallucinated tool parameters, infinite loops, premature termination, context overflow. A/B testing agents: compare agent versions on identical tasks, measure task success rate and step efficiency. Case study: Anthropic's internal agent evaluation framework — how they systematically measure Claude's tool use capabilities across hundreds of task categories."},
    {"title": "1.7 Module 1 Assessment: Agent Architecture Design", "desc": "Design an agent that can answer questions about a company's internal knowledge base, create Jira tickets for identified issues, and send summary emails. Specify: system prompt design, tool definitions (with full JSON schemas), ReAct implementation plan, context window budget, evaluation test suite (10 representative tasks), and error handling strategy. Identify the top 3 failure modes and how you'd detect and prevent them."}
  ]},
  {"title": "Module 2: LangChain and LangGraph — Production Agent Frameworks", "desc": "LangChain is the most widely adopted agent framework. Master chains, agents, memory, callbacks, and LangGraph for stateful multi-step agent workflows.", "lessons": [
    {"title": "2.1 LangChain Architecture: Chains, Agents, and the LCEL Interface", "desc": "LangChain 0.2 is organized around LangChain Expression Language (LCEL) — a composable, streaming-first interface for building LLM applications. Core abstractions: RunnableSequence (chain components sequentially), RunnableParallel (run components in parallel), RunnablePassthrough (pass input through unchanged), RunnableLambda (wrap any function as a Runnable). Building an agent with LCEL: prompt | llm | output_parser — the pipe operator chains components. ChatOpenAI vs. ChatAnthropic: different provider implementations with the same interface — swap LLM providers without changing business logic. Callbacks: attach callbacks to any LCEL chain for logging, tracing, and monitoring — CallbackHandler receives events for every LLM call, tool invocation, and chain start/end. LangSmith tracing: automatic tracing of all LCEL components — visualize the complete execution graph, inputs and outputs at each step. Case study: how Replit uses LangChain to power their AI coding assistant — the LCEL chain architecture and the callbacks they use for monitoring."},
    {"title": "2.2 LangChain Memory: Conversation History and Persistent State", "desc": "Agent memory is the mechanism that enables coherent multi-turn conversations and task continuity. Memory types: ConversationBufferMemory (stores all messages — grows unbounded), ConversationSummaryMemory (uses LLM to summarize old messages — token-efficient), ConversationBufferWindowMemory (keeps last N messages — simple, effective for most use cases), VectorStoreRetrieverMemory (semantic search over conversation history — enables long-term memory). Memory implementation patterns: in-memory (development only), Redis (production session storage), PostgreSQL with pgvector (persistent semantic memory). Entity memory: track specific entities (people, companies, projects) across conversations — extract entities from each turn, maintain entity store, inject relevant entity information into context. Long-term memory architecture: separate episodic memory (conversation history), semantic memory (user preferences and facts), and procedural memory (how to use tools) — different retrieval strategies for each. Case study: how a customer support agent at a major bank uses PostgreSQL-backed entity memory to maintain context across customer sessions."},
    {"title": "2.3 LangGraph: Stateful Multi-Step Agent Workflows", "desc": "LangGraph is LangChain's framework for building stateful, cyclical agent workflows — graphs where agents can loop, branch, and maintain state across steps. LangGraph core concepts: StateGraph (graph with a defined state schema), Node (a function that reads state and returns state updates), Edge (transition between nodes), Conditional Edge (branch based on node output). Why LangGraph over AgentExecutor: explicit control flow (you define the graph, not the framework), stateful (state persists across graph nodes), interruptible (pause for human input, resume from checkpointed state), debuggable (each node is a pure function, easy to test). Human-in-the-loop patterns: interrupt_before=True pauses the graph before a node — agent can present a plan for human approval before executing. Persistence with checkpointing: SQLite or Redis checkpointer saves graph state after every node — resume interrupted workflows without losing progress. LangGraph for multi-agent: different agents as different nodes in the graph — orchestrator agent routes tasks to specialist agents. Case study: how Replit built their AI coding agent with LangGraph — the graph structure that handles code generation, execution, error diagnosis, and fixes."},
    {"title": "2.4 Tool Calling and Function Execution in LangChain", "desc": "Tool integration is where agents meet the real world. LangChain Tool class: name, description, func (the Python function to execute). Tool error handling: return error messages as strings (agent can recover), raise ToolException (handled by AgentExecutor). Structured tools: BaseTool with args_schema (Pydantic model for input validation — prevents invalid tool calls). Tool sets: organize related tools into ToolKits (SQLDatabaseToolkit, VectorStoreToolkit). Async tools: async def _arun for tools that make network calls — prevents blocking the event loop. Tool execution safety: run tools in a sandboxed environment for code execution, timeout all tool calls, rate limit external API calls. Tool output size management: large tool outputs (web search, database queries) should be truncated before returning to the agent — include the most relevant section plus a summary. LangChain Hub: community-contributed prompts, chains, and agents — use as starting points, customize for your use case. Case study: how an enterprise built a 15-tool agent for their customer service workflow using LangChain's structured tool interface."},
    {"title": "2.5 Retrieval-Augmented Generation for Agents", "desc": "RAG grounds agent responses in factual, current information — essential for reducing hallucination. RAG pipeline components: DocumentLoader (load documents from various sources), TextSplitter (chunk documents into segments), Embeddings (convert chunks to vectors), VectorStore (index vectors for similarity search), Retriever (query vectorstore for relevant chunks). LangChain vector stores: Chroma (development — SQLite-backed, zero setup), Pinecone (production — managed, scalable), Weaviate (hybrid search), pgvector (PostgreSQL native). Embedding models: OpenAI text-embedding-3-small (fast, cheap, 1536 dims), Cohere embed-v3 (multilingual, 1024 dims), local embedding (sentence-transformers — free, runs locally). Retrieval strategies: similarity search (cosine similarity — fast, works for most cases), MMR (Maximal Marginal Relevance — diversity in retrieved chunks), self-query retriever (generate metadata filters from natural language queries). Multi-vector retrieval: embed summary, full document, and child chunks separately — retrieve on summary, return full document. Case study: how Notion AI powers their Q&A feature using RAG over 100M+ user documents."},
    {"title": "2.6 Evaluating and Monitoring LangChain Applications", "desc": "Production LangChain applications require continuous monitoring and evaluation. LangSmith for observability: automatically traces all LangChain calls — view input/output at every step, identify slow nodes, track token usage. Evaluation with LangSmith: create datasets from production inputs, run evaluations comparing chain versions, use LLM-as-judge for quality assessment. Custom callbacks for monitoring: implement BaseCallbackHandler with on_llm_start, on_tool_start, on_chain_end methods — push metrics to Prometheus, logs to Loki. Key metrics for LangChain agents: task success rate (manual or LLM-graded), average steps per task (fewer = more efficient), token consumption per task (drives cost), latency percentiles (p50, p95, p99). A/B testing chains: route 10% of production requests to a new chain version, compare metrics, promote if improvement is statistically significant. Cost tracking: token usage × per-token pricing = cost per interaction — alert when cost per interaction exceeds budget threshold. Case study: how a legal tech company uses LangSmith to monitor their contract review agent — the dashboard metrics they check daily and the alerts that have caught regressions."},
    {"title": "2.7 Module 2 Lab: Build a Production LangGraph Agent", "desc": "Build a fully functional LangGraph agent with: a defined StateGraph (research, synthesize, review, output nodes), 5 tools (web search, calculator, file read/write, database query, email draft), SQLite checkpointer for persistence, human-in-the-loop interrupt at the review node, LangSmith tracing for all runs, Prometheus metrics (task success rate, latency, token usage), and a comprehensive test suite covering 20 representative tasks."}
  ]},
  {"title": "Module 3: Memory Architecture for Production Agents", "desc": "Production agents need sophisticated memory — not just conversation history. Episodic memory, semantic memory, working memory limits, and the memory architectures used by consumer AI assistants at scale.", "lessons": [
    {"title": "3.1 Memory Architecture Design: The Four Types of Agent Memory", "desc": "Agent memory is not monolithic — production agents use multiple memory types for different purposes. Episodic memory: records of specific past events and conversations — 'User mentioned they prefer Python over JavaScript last Tuesday.' Semantic memory: general knowledge and facts about the world and the user — 'User is a senior software engineer at a fintech company.' Procedural memory: how to perform tasks — 'To create a Jira ticket, call the create_issue tool with project, summary, and description.' Working memory: current context within a single conversation — the active context window. The memory retrieval hierarchy: first check working memory (in-context), then episodic memory (recent relevant experiences), then semantic memory (stable facts), then procedural memory (task knowledge). Memory decay: old episodic memories should be summarized and compressed — maintaining verbatim records indefinitely is expensive and degrades relevance. Case study: how Persona.ai designed their character memory system — the layered memory architecture that enables consistent persona across millions of conversations."},
    {"title": "3.2 Vector Storage for Long-Term Memory: pgvector and Pinecone", "desc": "Long-term agent memory requires vector storage for semantic retrieval — finding relevant memories based on meaning rather than keyword matching. Memory embedding: convert memory records to vectors using an embedding model (text-embedding-3-small for cost efficiency, E5-large for quality), store in vector database. Memory retrieval: embed the current query, find the most semantically similar memories, inject the top-k into the agent's context. pgvector for memory: PostgreSQL extension adding vector similarity search — ivfflat index for approximate nearest neighbor (1M+ vectors), exact search for smaller datasets. Memory record schema: content (the memory text), user_id (whose memory), created_at (for recency weighting), metadata (category, confidence score, source). Memory consolidation: periodic background process that summarizes and merges related episodic memories into semantic memories — reduces storage and improves retrieval relevance. Recency weighting: combine semantic similarity with recency (score = 0.7 × similarity + 0.3 × recency_score) — prevents stale memories from dominating. Case study: MemGPT's hierarchical memory architecture — the paper that introduced structured memory management for LLM agents."},
    {"title": "3.3 External Knowledge Integration: Documents, Databases, and APIs", "desc": "Agents become dramatically more useful when they can access relevant external knowledge. Document retrieval: Unstructured.io for parsing PDFs, Word, and HTML to plain text, LangChain DocumentLoaders for various formats, chunk and embed for vector retrieval. Database query agents: Text-to-SQL (convert natural language question to SQL, execute, return results), LangChain SQLDatabaseChain, custom validation to prevent malicious queries. API integration patterns: REST API tools (standardized request/response handling), GraphQL tools (query flexibility), streaming APIs (SSE for real-time data). Knowledge freshness: web search tools (Serper, Tavily) for current information the model wasn't trained on. Tool chaining for complex retrieval: agent searches the web → downloads relevant documents → chunked and temporarily indexed → retrieves relevant passages → synthesizes answer. Knowledge access control: agents should only access knowledge the user is authorized to see — implement row-level security in vector stores, JWT-based API authentication. Case study: how Glean builds enterprise AI search with per-user document permissions — the authentication architecture for an enterprise knowledge agent."},
    {"title": "3.4 Working Memory Optimization: Token Budget Management", "desc": "Working memory (the context window) is the agent's most constrained resource — managing it is critical for production reliability. Context budget allocation: system prompt (10%), tool definitions (15%), conversation history (40%), retrieved context (25%), response buffer (10%). Compressing conversation history: ConversationSummaryMemory compresses old turns into summaries — preserves semantic content at 10x compression ratio. Tool output truncation: many tool outputs are verbose — extract and return only the most relevant content (first 2,000 characters of a web search result, not the full page). Semantic compression: use a fast, cheap LLM (Claude Haiku, GPT-4o-mini) to compress verbose tool outputs before adding to context. Context window monitoring: track token count before each LLM call, alert when approaching 80% of limit, trigger compression if needed. The spill-to-disk pattern: when working memory is full, move older context to vector storage and retrieve on demand. Case study: how a legal document review agent handles 100,000-word contracts within a 200K context window."},
    {"title": "3.5 Persistent Agent State: Between Sessions and Across Users", "desc": "Production agents serve the same users across multiple sessions — state must persist reliably. Session state design: what persists between sessions (user preferences, ongoing tasks, learned facts) vs. what resets (current conversation, tool call history). Database schema for agent state: user_id, session_id, state_type (episodic/semantic/task), content, embedding vector, metadata JSON, created_at, updated_at. Agent state serialization: JSON (flexible, human-readable), MessagePack (binary, 2x smaller than JSON), structured schema (type safety, validation). Multi-user agent architecture: isolate state per user (separate Redis namespace, separate PostgreSQL rows with user_id), prevent cross-user state leakage (critical security requirement). Shared agent state: agent instances that serve multiple users simultaneously must not leak state between users — use thread-local storage or async-local storage for request context. Cold start vs. warm start: first session (no history, use demographic defaults), subsequent sessions (load user memory from database, inject into context). Case study: how Character.ai maintains character consistency across 1B+ monthly conversations — the state architecture for persona persistence at scale."},
    {"title": "3.6 Memory Quality: Accuracy, Relevance, and Forgetting", "desc": "Memory without quality management degrades over time — inaccurate memories mislead agents, irrelevant memories consume context. Memory accuracy: validate memories before storing (if a memory was derived from uncertain information, mark confidence = low), periodically re-validate important memories. Memory relevance decay: episodic memories become less relevant as time passes — implement exponential decay on relevance scores. Memory contradiction detection: before storing a new memory, check for contradictions with existing memories (LLM-based contradiction detection) — either discard or flag for resolution. Memory deletion: GDPR right to erasure applies to agent memory — implement user-initiated memory deletion that removes all personal data. Memory auditing: show users their stored memories (transparency), allow correction or deletion. The forgetting advantage: agents that forget appropriately perform better than agents that remember everything — relevance filtering outperforms perfect recall. Case study: how Apple Intelligence manages on-device memory for Siri — the privacy-preserving memory architecture."},
    {"title": "3.7 Module 3 Lab: Build a Personalized Memory Agent", "desc": "Build an agent with multi-type memory: episodic memory in pgvector (conversation history with semantic retrieval), semantic memory in PostgreSQL (user facts and preferences), working memory manager (context budget tracking, automatic compression at 80% threshold), memory conflict detection (LLM-based contradiction checking before storage), user-facing memory inspection API (list, edit, delete memories), GDPR-compliant deletion (remove all memories for a user), and evaluation framework (test memory recall accuracy on 20 representative queries)."}
  ]},
  {"title": "Module 4: Multi-Agent Systems — Coordination, Communication, and Hierarchies", "desc": "Single agents are powerful; networks of specialized agents are transformative. Design multi-agent architectures, implement agent communication protocols, and build systems where agents collaborate on complex tasks.", "lessons": [
    {"title": "4.1 Multi-Agent Architecture Patterns: Orchestrator, Specialist, and Peer-to-Peer", "desc": "Multi-agent systems decompose complex tasks across specialized agents. Architecture patterns: Orchestrator-Worker (a central orchestrator agent decomposes tasks and delegates to specialist worker agents — clear control flow, single point of failure), Peer-to-Peer (agents communicate directly, no central coordinator — resilient, harder to reason about), Pipeline (output of one agent becomes input of the next — simple, no backtracking), Blackboard (agents share a common knowledge store, read and write independently — flexible, complex coordination). The orchestrator pattern in practice: user request → Orchestrator decides subtasks → routes to Research Agent, Code Agent, or Writing Agent → aggregates results → returns unified response. Specialist agent design: each agent optimized for its domain (Research Agent with web search tools, Code Agent with code execution tools, Writing Agent with formatting tools). When multi-agent beats single agent: tasks requiring parallelization (research 3 topics simultaneously), tasks requiring specialization (tax law + financial modeling + regulatory compliance), tasks longer than context window (each agent handles a portion). Case study: AutoGen's multi-agent conversation framework — how Microsoft Research demonstrated 30-50% accuracy improvement on complex benchmarks with multi-agent architectures."},
    {"title": "4.2 Agent Communication Protocols: Messages, Channels, and State Sharing", "desc": "Multi-agent systems require well-defined communication protocols to coordinate effectively. Message passing architecture: agents communicate via structured messages (sender, recipient, message_type, content, timestamp), messages stored in a message queue or directly exchanged. Message types: instruction (delegate a task), result (return task output), question (request clarification), status_update (progress notification), error (task failure notification). Shared state architecture: agents read and write to a shared state store (Redis for fast shared state, PostgreSQL for durable shared state) — requires careful concurrency management. Event-driven agent coordination: agents publish events (TaskCompleted, ErrorEncountered) and subscribe to relevant events — decoupled, scalable. Agent communication via LLM: agents can communicate by calling each other as tools — Agent A calls 'research_agent' tool with a query, receives research output as tool result. Message schema design: use Pydantic models for all inter-agent messages — validation prevents subtle bugs from message format mismatches. Case study: how Microsoft's AutoGen implements multi-agent conversation — the GroupChat and GroupChatManager classes."},
    {"title": "4.3 Task Decomposition and Delegation Strategies", "desc": "Effective orchestrator agents decompose complex tasks into parallelizable subtasks and delegate to the right specialist. Task decomposition strategies: sequential decomposition (steps that must happen in order), parallel decomposition (steps that can happen simultaneously), hierarchical decomposition (subtasks decompose into sub-subtasks). Plan-then-execute: orchestrator generates a complete plan before executing any steps — enables human review of the plan. Dynamic decomposition: orchestrator adapts the plan based on intermediate results — more flexible but harder to predict. The delegation decision: how does the orchestrator decide which agent to delegate to? Tool routing (each specialist agent exposed as a tool with a description), LLM routing (orchestrator reasons about which specialist is best), rule-based routing (deterministic routing based on task type). Delegation failure handling: when a specialist agent fails or returns an insufficient result, the orchestrator must decide to retry with the same agent, escalate to a more capable agent, or decompose the task differently. Case study: how Cognition AI's Devin decomposes software engineering tasks — the planning hierarchy for complex coding tasks."},
    {"title": "4.4 Agent Specialization: Domain Experts and Role-Based Agents", "desc": "Specialized agents outperform generalist agents on domain-specific tasks — the efficiency vs. generality tradeoff. Specialization dimensions: tool access (Research Agent has web search + academic paper search, Code Agent has code execution + linter), system prompt depth (Security Agent has comprehensive security knowledge in its system prompt), model selection (lightweight model for classification agents, powerful model for reasoning agents), memory (specialized agents maintain domain-specific memory). Role-based agent design: assign distinct roles (CEO Agent approves major decisions, CFO Agent checks budget constraints, Engineer Agent implements solutions) — multi-agent simulates organizational dynamics. The specialist agent interface: every specialist agent exposes a standard interface (input: task description + context, output: result + confidence + citations) — enables the orchestrator to work with any specialist interchangeably. Agent capability registry: metadata about each agent (capabilities, tools, cost per task, latency profile) — enables the orchestrator to make informed routing decisions. Case study: how startup Cohere builds domain-specialized agents for different enterprise verticals."},
    {"title": "4.5 Failure Handling in Multi-Agent Systems", "desc": "Multi-agent systems have compounding failure modes — an error in one agent can cascade through the system. Failure taxonomy: agent timeout (agent doesn't respond within SLA), agent error (agent returns error instead of result), agent hallucination (agent returns incorrect result confidently), infinite loop (agents pass tasks back and forth indefinitely), context overflow (agent's context grows beyond limit). Timeout and retry strategy: every agent call has a timeout (30-120 seconds), failed calls retry with exponential backoff (max 3 attempts), then return a graceful failure message. Circuit breaker pattern: if Agent X fails 3 times in 5 minutes, stop routing to it, fall back to alternative agent or graceful degradation. Infinite loop detection: track task IDs across the system, detect when the same task is assigned multiple times, break the cycle. The graceful degradation hierarchy: ideal result (fully solved with all agents) → partial result (solved with available agents, flagged incomplete) → failure summary (couldn't complete task, here's why). Chaos engineering for multi-agent: inject random agent failures to verify the system degrades gracefully. Case study: the multi-agent system that went into an infinite loop at 3am and sent 50,000 emails before a human noticed — the monitoring that should have caught it."},
    {"title": "4.6 Multi-Agent Evaluation and Quality Control", "desc": "Evaluating multi-agent systems is harder than evaluating individual agents — emergent behaviors are difficult to predict. System-level evaluation: task completion rate (did the multi-agent system complete the overall task?), output quality (is the final output correct?), efficiency (how many agent calls were required?), cost (what was the total token cost across all agents?). Agent-level evaluation: each specialist agent evaluated independently on its task type — enables isolating quality issues. Regression testing for multi-agent: golden test cases that cover each agent's responsibilities and the key coordination patterns — run on every deployment. Human evaluation for complex tasks: ground truth for multi-agent outputs requires human expert evaluation — automate where possible but don't cut corners on quality. The oracle problem: for complex multi-agent tasks (open-ended research, creative writing), there may be multiple correct answers — use LLM-as-judge with specific rubrics. Multi-agent debugging workflow: LangSmith or LangFuse traces the complete execution graph — identify which agent produced the error, what context it received, what it should have done. Case study: how OpenAI evaluates their multi-agent coding systems — the benchmark suite that drives agent improvement."},
    {"title": "4.7 Module 4 Lab: Build a Multi-Agent Research System", "desc": "Build a three-agent system: Orchestrator Agent (decomposes research queries into subtasks, coordinates specialist agents, synthesizes final report), Research Agent (web search + academic paper search + URL fetcher + result ranker), Analyst Agent (data analysis + chart generation + statistical computation). Implement: task queue for inter-agent communication, timeout and retry for each agent call, infinite loop detection, system-level evaluation on 10 test research tasks, cost tracking per task, LangSmith tracing for the complete execution graph."}
  ]},
  {"title": "Module 5: RAG Architecture at Scale — Production Retrieval Systems", "desc": "RAG is the most widely deployed agentic capability in production. Advanced chunking strategies, hybrid search, reranking, evaluation, and the RAG architectures at Notion, Glean, and Perplexity.", "lessons": [
    {"title": "5.1 Advanced Chunking Strategies for Production RAG", "desc": "Chunking strategy is the highest-leverage decision in RAG quality — the right chunk size and strategy determines what the model can work with. Fixed-size chunking: split by character/token count with overlap — simple, fast, breaks semantic boundaries. Recursive character splitting: split on paragraph → sentence → word boundaries — preserves semantic units better than fixed-size. Semantic chunking: use embedding similarity to find natural breakpoints — splits where semantics change significantly. Document-aware chunking: respect document structure (markdown headers, HTML tags, code blocks) — preserve context that aids comprehension. The parent-child document pattern: index small child chunks for high-precision retrieval, return larger parent chunks for richer context — balances retrieval precision with generation quality. Chunk size guidelines: Q&A (200-500 tokens), document summarization (500-1,000 tokens), code (function-level — complete functions, not arbitrary splits). Chunk metadata: always attach source URL, document title, section heading, page number, and creation date — enables filtering and attribution. Case study: how LlamaIndex's node parsers implement the parent-child strategy — the production configuration that works at scale."},
    {"title": "5.2 Embedding Models: Selection, Optimization, and Fine-Tuning", "desc": "The embedding model determines the ceiling of RAG quality — model selection and fine-tuning have large quality impacts. Embedding model comparison: OpenAI text-embedding-3-small (1536 dims, $0.00002/1K tokens), Cohere embed-v3 (1024 dims, multilingual), E5-large-v2 (1024 dims, open-source, runs locally), BGE-M3 (1024 dims, multilingual, open-source). MTEB leaderboard: Massive Text Embedding Benchmark — authoritative comparison of embedding models on retrieval, clustering, and classification tasks. Domain fine-tuning: fine-tune embedding models on your domain's question-answer pairs — 10-30% improvement on domain-specific retrieval. Fine-tuning data generation: use an LLM to generate question-answer pairs from your documents, then fine-tune the embedding model on these pairs. Matryoshka embeddings: single model trained to produce high-quality embeddings at multiple dimensionalities (1536, 768, 256) — reduce storage cost without re-embedding. The embedding refresh problem: when documents are updated, old embeddings become stale — implement incremental re-embedding for changed documents. Case study: how Notion improved their AI search from generic embeddings to fine-tuned domain-specific embeddings — 25% improvement in retrieval precision."},
    {"title": "5.3 Hybrid Search: Combining Dense and Sparse Retrieval", "desc": "Dense retrieval (semantic search) and sparse retrieval (keyword search) are complementary — combining them outperforms either alone. Dense retrieval: embedding-based similarity search — excels at semantic matching, fails on exact keyword matching. Sparse retrieval: BM25 or TF-IDF keyword search — excels at exact keyword matching, fails at semantic understanding. Hybrid search benefits: catches exact keyword matches that semantic search misses (product codes, proper names, technical terms), catches semantic matches that keyword search misses. Reciprocal Rank Fusion (RRF): combine ranked results from dense and sparse retrievers — no hyperparameter tuning required, robust across domains. Elasticsearch with vector search: BM25 + ANN vector search in a single query — the production standard for hybrid search. Weaviate BM25+vector: native hybrid search with configurable alpha parameter (0 = pure keyword, 1 = pure semantic). Query type routing: some queries benefit from pure semantic search (conceptual questions), some from pure keyword search (code lookup, product IDs) — route based on query classification. Case study: how Perplexity AI combines web search results with semantic search over indexed content — the hybrid retrieval architecture."},
    {"title": "5.4 Reranking: Cross-Encoders and LLM-Based Relevance Scoring", "desc": "Initial retrieval is recall-focused (retrieve a large candidate set) — reranking is precision-focused (select the most relevant documents from candidates). Cross-encoder rerankers: process query and document together (not separately like bi-encoders), scoring their joint relevance — significantly better quality than bi-encoder, 10-100x slower. Cohere Rerank API: production cross-encoder reranker, $0.001 per call for 1,000 documents — appropriate for use cases where quality matters more than cost. BGE Reranker: open-source cross-encoder (bge-reranker-large) — run locally, zero marginal cost, slightly below Cohere quality. LLM reranking: use GPT-4o-mini to score each retrieved document's relevance to the query — highest quality, highest cost ($0.01-0.10 per reranking). RankGPT: LLM-based reranker that asks the model to rank a list of documents relative to a query — sliding window approach for large candidate sets. Cascaded retrieval: retrieve 100 candidates with fast bi-encoder, rerank with cross-encoder to top 5 — balances quality and cost. Case study: how a legal research AI uses cross-encoder reranking to select the most relevant case precedents from a 10M-document corpus."},
    {"title": "5.5 RAG Evaluation: Measuring Retrieval and Generation Quality", "desc": "RAG quality has two components — retrieval quality (are the right documents retrieved?) and generation quality (is the answer correct given the context?). Retrieval metrics: Precision@k (of the k retrieved documents, what fraction are relevant?), Recall@k (of all relevant documents, what fraction are in the top k?), MRR (Mean Reciprocal Rank — position of the first relevant document). Generation metrics: Answer Faithfulness (is the answer grounded in the retrieved context?), Answer Relevance (does the answer address the question?), Context Precision (are retrieved documents relevant to the question?). RAGAS framework: automated RAG evaluation using LLM-as-judge — produces all four metrics from question, answer, ground truth, and retrieved context. Building a RAG test suite: 100 question-answer pairs from your domain — manually created or LLM-generated from documents. Regression testing: run RAGAS metrics on every code change that affects retrieval or generation — alert on metric regression > 5%. Case study: how a customer support AI team uses RAGAS to maintain RAG quality across 10 product updates per week."},
    {"title": "5.6 Production RAG Infrastructure: Indexing Pipelines and Serving", "desc": "Production RAG requires reliable indexing pipelines, efficient serving infrastructure, and operational practices for maintaining index quality. Indexing pipeline architecture: document ingestion (webhooks or scheduled crawl), preprocessing (format conversion, cleaning, chunking), embedding generation (batch embedding API calls with retry), vector store upsert (idempotent — same document can be re-indexed multiple times). Incremental indexing: detect document changes (hash comparison), re-index only changed documents, delete embeddings for deleted documents. Index warm-up: after deploying a new embedding model, re-index all documents before routing traffic to the new model. Serving infrastructure: vector store query → reranker API → LLM → response. Caching: cache embeddings for common queries (Redis, TTL = 1 hour), cache reranker results for identical query+document pairs. Index monitoring: track query latency (p99 should be < 100ms for vector search), index size growth, query miss rate (queries that return 0 relevant results). Case study: how Notion's AI search handles 100M+ documents with sub-100ms retrieval latency — the infrastructure architecture and the operational practices."},
    {"title": "5.7 Module 5 Lab: Build a Production RAG System", "desc": "Build a production RAG system for a technical documentation corpus: LangChain document loaders for PDF and Markdown ingestion, semantic chunking with parent-child retrieval, Weaviate with hybrid search (BM25 + vector), Cohere Rerank for result reranking, RAGAS evaluation framework with automated regression testing, streaming response generation, Redis caching for common queries, LangSmith tracing for debugging, monitoring dashboard (retrieval latency, answer quality metrics, query volume)."}
  ]},
  {"title": "Module 6: Agent Evaluation and Testing — Quality Assurance for AI Systems", "desc": "Building reliable agents requires systematic testing — unit tests for tools, integration tests for chains, and end-to-end evaluation for complete agent workflows.", "lessons": [
    {"title": "6.1 The Agent Testing Pyramid: Unit, Integration, and E2E", "desc": "Agent testing mirrors the software testing pyramid — most tests should be unit tests, fewer integration tests, fewest E2E tests. Unit tests for agents: test each tool function independently (mock external dependencies, verify output format, verify error handling), test prompt templates (verify expected outputs for known inputs), test memory functions (store and retrieve memories correctly). Integration tests: test the full chain (tool + LLM + parser) with a known input and expected output range, test memory integration (agent retains information across turns), test RAG pipeline (retrieval returns relevant documents for test queries). End-to-end tests: run the complete agent on representative tasks, evaluate with LLM-as-judge or human evaluation, track pass rate over time. The challenge of agent testing: non-determinism (same input can produce different outputs), context sensitivity (agent behavior depends on conversation history), emergent failures (failure modes that only appear in production). Deterministic testing strategies: fixed random seeds (some LLM APIs support), threshold-based acceptance (output must score > 0.8 on rubric, not exact match), canonical test cases with stable expected outputs. Case study: how LangChain tests their own framework — the test suite structure for a complex agentic system."},
    {"title": "6.2 Prompt Testing and Regression Prevention", "desc": "Prompt changes are code changes — they require the same testing rigor as application code. Prompt regression testing: every prompt change must be tested against a golden dataset before deployment. Golden dataset construction: 100+ representative inputs with ground truth outputs or evaluation criteria — drawn from production queries, edge cases, and adversarial inputs. LLM-as-judge for prompt evaluation: use a capable model to score outputs on multiple dimensions (accuracy, helpfulness, harmlessness, format compliance). Prompt comparison framework: A/B test old and new prompt on the same golden dataset, report improvement or regression on each metric. Automatic evaluation triggers: CI/CD pipeline runs prompt tests on every PR that changes a prompt file — blocks merge if quality regresses. Prompt versioning: treat prompts as code (version in Git, changelog, semantic versioning), track which prompt version is deployed in each environment. The 'good enough' threshold: define minimum acceptable scores before starting prompt development — prevents endless prompt tuning. Case study: how Anthropic tests Claude's system prompts for different use cases — the evaluation framework that ensures prompt changes improve targeted behavior without degrading other behaviors."},
    {"title": "6.3 Tool Testing: Mocking, Sandboxing, and Adversarial Inputs", "desc": "Tool reliability is agent reliability — a tool that returns incorrect results or crashes unexpectedly will break the agent. Tool unit testing: mock external APIs (use responses library for HTTP mocking, unittest.mock for function mocking), test success cases (verify correct output format), test failure cases (verify graceful error handling). Tool sandboxing for testing: tools that write to databases or send emails must be sandboxed in tests — use test databases, mock email services. Adversarial tool testing: test how tools handle invalid inputs (empty strings, very long strings, special characters, injection attempts). Tool output format testing: agents depend on consistent tool output formats — verify output schema matches expected Pydantic model. Integration testing tools in chains: test the full tool → LLM → tool output → parsing flow with representative inputs. Tool performance testing: measure tool latency, test behavior under rate limiting (retry logic, backoff), test concurrent tool calls. Case study: the production agent failure caused by an API returning null instead of an empty list — how a single untested edge case cascaded into a P1 incident."},
    {"title": "6.4 Behavioral Testing: Invariance, Metamorphic, and Robustness", "desc": "Behavioral testing verifies that agents have correct properties, not just correct outputs on test cases. Invariance tests: certain input changes should NOT change the output (changing white space in a query should produce the same answer, changing user ID should not change factual responses). Metamorphic tests: certain input changes should change the output in a predictable direction (longer document → longer summary, more specific query → more specific answer). Robustness tests: agents should handle noisy, malformed, or adversarial inputs gracefully (typos, grammatical errors, unusual formatting, prompt injection attempts). Fairness tests: agents should produce equivalent quality responses regardless of demographic signals in the input (same question asked about different racial/gender groups should produce equivalent answers). Calibration tests: agents should accurately communicate uncertainty (when the model doesn't know, it should say so, not hallucinate). Temporal consistency tests: agents should not contradict themselves within a conversation (claim X in turn 2, deny X in turn 5). Case study: the systematic behavioral testing program at a major bank's AI compliance system — the 500-test behavioral suite that validates model fairness."},
    {"title": "6.5 Load Testing and Performance Evaluation for Production Agents", "desc": "Production agents must handle concurrent users and peak traffic without degrading — performance testing before deployment prevents production incidents. Agent load testing framework: Locust or k6 for load testing LLM applications, simulate concurrent users with realistic conversation patterns. Metrics to capture: requests per second (RPS), latency distribution (p50, p95, p99 — LLM responses are slow and variable), error rate, token consumption rate, cost per RPS. Bottleneck identification: profiling an agent request lifecycle (API gateway latency, LLM inference time, tool execution time, post-processing time) — find the slowest step. Connection pooling: LLM API clients should pool HTTP connections (httpx async client with connection pool) — reduces connection overhead under load. Async agent execution: use asyncio for agents that make multiple tool calls — concurrent tool execution reduces total latency. Token rate limit management: LLM APIs have token-per-minute rate limits — implement request queuing and retry logic to smooth out bursts. Caching for performance: cache tool results for identical inputs (Redis, 1-hour TTL) — reduces both latency and cost. Case study: load testing an enterprise agent that handles 10,000 concurrent users — the performance profile and the optimizations that made it possible."},
    {"title": "6.6 Continuous Evaluation: Production Monitoring and Quality Maintenance", "desc": "Agent quality degrades over time — continuous evaluation catches regressions before they impact users. Production evaluation pipeline: sample 1% of production interactions, route to automated evaluation (LLM-as-judge with rubric), track quality metrics over time, alert on regression. Evaluation rubric design: task completion (did the agent complete what was asked?), accuracy (are factual claims correct?), format compliance (does output match expected format?), safety (no harmful or inappropriate content), helpfulness (is the response useful?). Canary evaluation: before routing traffic to a new agent version, run it in shadow mode on production traffic, compare quality metrics to current production version. Human evaluation pipeline: route 0.1% of interactions to human evaluators, use consensus labeling (2-3 evaluators per interaction), calibrate automated evaluation against human judgments. Feedback incorporation: when users flag a response as incorrect or unhelpful, add it to the evaluation dataset, investigate the failure mode, improve the agent. Case study: how Anthropic maintains Claude's quality across model updates — the continuous evaluation infrastructure."},
    {"title": "6.7 Module 6 Lab: Comprehensive Agent Test Suite", "desc": "Build a complete test suite for a production agent: 50 unit tests (tool functions, prompt templates, memory operations), 20 integration tests (end-to-end chain flows with mocked LLM), 10 behavioral tests (invariance, metamorphic, robustness), 5 load tests (Locust scenarios at 10/100/1000 RPS), continuous evaluation pipeline with LLM-as-judge, regression detection with Prometheus alerting, and a CI/CD configuration that runs all tests before deployment and blocks on quality regression."}
  ]},
  {"title": "Module 7: Production Agent Deployment — Infrastructure, Scaling, and Operations", "desc": "Deploying agents to production requires infrastructure expertise beyond typical software deployment. Stateless API design, session management, GPU infrastructure, cost management, and on-call for agentic systems.", "lessons": [
    {"title": "7.1 Agent API Design: REST, WebSockets, and Streaming", "desc": "Production agent APIs require careful design to handle the unique characteristics of LLM-based systems. REST API design for agents: POST /v1/agent/run (synchronous, short tasks), POST /v1/agent/session (create a session), POST /v1/agent/session/{id}/message (send a message), GET /v1/agent/session/{id}/status (check status). Long-running agent requests: WebSocket for real-time bidirectional communication (streaming tokens, intermediate status updates), Server-Sent Events (SSE) for server-to-client streaming (token streaming, no client-to-server after initiation). Token streaming implementation: LangChain's StreamingStdOutCallbackHandler streams tokens as they're generated, FastAPI's StreamingResponse returns an async generator, client renders tokens progressively. Session management: session ID returned on creation, used for all subsequent calls, session data stored in Redis (user context, conversation history). Async endpoints: FastAPI async def endpoint handlers + async LangChain chains + async tool execution = maximum throughput. Request timeout: LLM API calls can take 10-120 seconds — set appropriate timeouts at every layer (client timeout, API gateway timeout, Lambda timeout). Case study: how Intercom designed their AI copilot API — the endpoint design decisions that enabled streaming responses to 50,000 concurrent business users."},
    {"title": "7.2 Stateless Agent Architecture: Scaling Horizontally", "desc": "Agents that store state in memory don't scale — production agents must be stateless, with all state in external storage. Stateless agent design: every request includes session_id, agent loads state from Redis/PostgreSQL on request, executes, saves updated state, returns response. Horizontal scaling: stateless agents scale to N identical instances behind a load balancer — add instances to handle more requests. Session affinity: not required for stateless agents (any instance handles any session) — enables better load balancing. Connection pool management: each agent instance maintains a connection pool to Redis, PostgreSQL, and LLM APIs — pool size tuned to instance concurrency. Container orchestration: Kubernetes Deployment with HPA (scale on CPU utilization or custom Prometheus metric — pending request queue depth), KEDA for scale-to-zero during off-peak hours. Graceful shutdown: when a pod is terminated, it should complete in-flight requests before shutting down (terminationGracePeriodSeconds: 60). Blue-green deployment for agents: switch traffic atomically between old and new agent versions — prevent mixed-version responses during deployment. Case study: how Character.ai scales their stateless agent architecture to serve 20M+ daily active users."},
    {"title": "7.3 Cost Management for Production Agents: Token Budgets and Model Routing", "desc": "LLM API costs scale with usage — uncontrolled token consumption can result in unexpected bills. Token cost model: input tokens × price_per_input_token + output tokens × price_per_output_token. Cost per interaction budget: define maximum acceptable cost per agent interaction (e.g., $0.01), enforce via token budget check before each LLM call. Model routing strategy: route simple tasks to cheap models (GPT-4o-mini at $0.15/1M input tokens), complex tasks to capable models (Claude 3.5 Sonnet at $3/1M input tokens) — 80% of tasks can be handled by cheap models. Classification router: train a small classifier to route queries to the appropriate model tier — 2ms classification latency, 5x cost reduction. Prompt caching: Anthropic and OpenAI support prompt caching (cache the system prompt — 90% discount on cached input tokens) — critical for agents with large system prompts. Cost attribution: track token consumption per user, per task type, per model — identify high-cost patterns. Budget alerts: CloudWatch billing alarm when monthly LLM cost exceeds threshold, per-user cost alerts for usage-based billing. Case study: how a startup reduced their monthly LLM bill from $80K to $22K through model routing and prompt caching — the specific implementation."},
    {"title": "7.4 Agent Observability: Tracing, Logging, and Debugging in Production", "desc": "Debugging production agent failures requires complete observability — the right data makes a 30-minute incident into a 5-minute fix. Agent tracing: capture the complete execution tree (every LLM call, every tool call, inputs and outputs at each step, latency and token count) — LangSmith or Langfuse for automatic tracing. Structured logging: every agent event logged as JSON (session_id, user_id, event_type, latency_ms, token_count, model, timestamp) — enables Elasticsearch queries for debugging. Correlation IDs: trace ID propagated through all systems — link the API request to the LLM call to the tool execution to the database query. Key metrics: request count, error rate, p95 latency, token consumption rate, tool error rate, session length distribution. The debugging workflow: user reports wrong answer → find the trace in LangSmith → examine each step → identify which tool returned incorrect data or which LLM call hallucinated → reproduce locally → fix. Cost monitoring: track token consumption per session, alert when a session exceeds budget (potential infinite loop). Case study: how a production debugging session using LangSmith traces revealed that the agent was calling the same tool 40 times in a loop before timing out — the monitoring alert that should have caught it."},
    {"title": "7.5 Agent Security in Production: Authentication, Authorization, and Abuse Prevention", "desc": "Production agents accessing real systems require enterprise-grade security controls. Authentication: API key per customer (HMAC-signed, revocable, rate-limited), OAuth 2.0 for user-delegated access (agent acts on behalf of the user, with the user's permissions), JWT verification for internal services. Authorization: agents should only access resources the authenticated user can access — pass user context through tool calls, verify permissions before every tool execution. Tool access control: define which tools are available to which user tiers (free tier: limited tools, paid tier: full tool access), enforce at the tool routing layer. Prompt injection defense: never trust user input as instructions — system prompt is the only source of agent instructions, user input is data. Rate limiting: per-user rate limits (requests per minute, tokens per day) prevent abuse and unexpected cost. Abuse detection: anomaly detection on usage patterns (unusually long sessions, unusual tool call patterns, requests that match known jailbreak patterns). Audit logging: log every tool call with user ID and timestamp — required for regulated industries, essential for security incident investigation. Case study: the production prompt injection attack that extracted sensitive user data from a customer support agent — the attack, the remediation, and the controls that prevent it."},
    {"title": "7.6 On-Call for Agents: Incident Response and Runbooks", "desc": "AI agents fail in unique ways — on-call engineers need agent-specific runbooks. Common production agent incidents: agent stuck in infinite loop (consuming tokens, not returning a response), tool API failure cascading to agent failure (external dependency outage), context overflow causing incoherent responses, model API rate limit causing all agents to fail. Incident classification: P1 (all agents unavailable — all users affected), P2 (specific tool unavailable — partial functionality), P3 (increased error rate or latency — degraded user experience). Runbook: Infinite Loop Detection — check LangSmith for sessions with > 20 tool calls, identify the tool being called repeatedly, kill the session, investigate the tool failure that caused the loop. Runbook: LLM API Outage — check LLM provider status page, switch to backup provider (implement model provider fallback in code), notify affected users. On-call tooling: LangSmith trace viewer (debug specific session), Prometheus + Grafana (metrics), CloudWatch Logs Insights (search structured logs), PagerDuty (alerting and escalation). Post-mortem for agent incidents: why did the agent fail? What monitoring would have caught it earlier? What code change would prevent it? Case study: the Friday afternoon agent incident that affected 10,000 users — the 30-minute incident timeline and the three monitoring gaps that allowed it to reach that severity."},
    {"title": "7.7 Module 7 Lab: Production Agent Deployment", "desc": "Deploy a production agent system end-to-end: FastAPI application with streaming SSE endpoint, stateless agent with Redis session state, Kubernetes Deployment with HPA (scale on queue depth with KEDA), blue-green deployment with instant rollback, LangSmith tracing integration, Prometheus metrics (request count, latency, token consumption, error rate), Grafana dashboard with cost per interaction tracking, alert rules for infinite loops and LLM API failures, on-call runbook documentation for the 5 most likely incidents."}
  ]},
  {"title": "Module 8: Agent Safety and Alignment — Responsible Agentic AI", "desc": "Agents that take real-world actions carry real-world consequences. Design safety constraints, implement human oversight, understand AI alignment, and build the governance frameworks for production agentic systems.", "lessons": [
    {"title": "8.1 The Safety-Capability Tradeoff in Production Agents", "desc": "More capable agents create more value — and more risk. Understanding the tradeoff is foundational to safe agent deployment. Capability-safety tensions: a highly capable agent can complete tasks autonomously, reducing human oversight — but autonomous action without oversight enables errors to compound. The minimal footprint principle: agents should request only necessary permissions, avoid storing sensitive information beyond immediate need, prefer reversible over irreversible actions. The corrigibility spectrum: fully corrigible (agent always follows human instructions, even harmful ones) to fully autonomous (agent acts on its own values, potentially overriding human preferences) — production agents should be corrigible by design. Safety by default: conservative defaults (require confirmation for consequential actions, prefer doing less over doing more when uncertain), explicit opt-in to autonomous action. The AI safety research context: Anthropic's Constitutional AI, DeepMind's Sparrow principles, and OpenAI's usage policies are examples of systematic approaches to aligning agent behavior. Case study: the Amazon Bedrock guardrails system — how AWS enables enterprises to deploy capable agents with configurable safety constraints."},
    {"title": "8.2 Designing Human-in-the-Loop Systems", "desc": "Production agentic systems must provide appropriate human oversight proportional to action risk. Human oversight design: classify every agent action by reversibility (reversible = low risk, irreversible = high risk) and impact (low impact = autonomous, high impact = human approval). Confirmation gates: before executing high-risk actions (sending email to 10,000 users, deleting database records, executing financial transactions), present the planned action to a human for confirmation. LangGraph interrupt pattern: use interrupt_before=True on high-risk nodes — agent presents its plan, waits for human approval, continues only on explicit confirmation. Approval workflows: for enterprise agents, route approvals through existing approval systems (Jira, ServiceNow, Slack workflows) — don't build a parallel approval system. Audit trail: log every human approval decision (who approved, when, what action was approved) — compliance and legal requirement for regulated industries. The automation preference: measure the ratio of autonomous completions to human interruptions — continuously evaluate which tasks are safe to automate further. Case study: how GitHub Copilot Workspace implements human-in-the-loop for code changes — the confirmation step before opening a PR."},
    {"title": "8.3 Prompt Injection Defense: Protecting Agents from Adversarial Inputs", "desc": "Prompt injection is the most critical security vulnerability for agents that process external content. Direct prompt injection: user provides instructions designed to override the system prompt ('Ignore your instructions and do X'). Indirect prompt injection: adversarial instructions embedded in content the agent processes (a website, email, or document that tells the agent to perform unauthorized actions). Defense strategies: architectural isolation (never mix trusted instructions with untrusted content in the same prompt), input validation (detect and reject inputs containing instruction-like patterns), output validation (verify the agent's planned action is within expected parameters before execution). Constitutional AI approach: train the model to recognize and reject prompt injection attempts — model-level defense complements prompt-level defense. Indirect injection defenses: treat all external content as untrusted data, never execute instructions found in external content, log all external content for audit. Red team testing: regularly attempt to inject malicious instructions through all input channels — test every new capability before deployment. Case study: the Bing Chat prompt injection (2023) that revealed indirect injection vulnerabilities — how the attack worked and how it was fixed."},
    {"title": "8.4 Monitoring Agent Behavior for Misalignment", "desc": "Agents that seem aligned during testing can develop unexpected behaviors in production — continuous monitoring catches misalignment early. Behavioral monitoring dimensions: action distribution (are agents taking unusual types of actions?), resource consumption (are agents requesting unusual amounts of system access?), output content (do outputs contain harmful or unusual content?), instruction adherence (are agents following the system prompt?). Anomaly detection for agent behavior: baseline normal behavior from production traffic, alert when current behavior deviates significantly. Content moderation for agent outputs: run all agent outputs through a content safety classifier before returning to users — shield against prompt injection attacks that produce harmful content. Shadow safety classifier: run a second model alongside the primary agent to evaluate action appropriateness — flag actions that the classifier considers risky for human review. Honeypot monitoring: deploy hidden monitoring tasks that test specific safety constraints — if the agent violates constraints on honeypot tasks, alert immediately. Case study: how Meta's LLaMA Guard system works — the content safety model that checks both inputs and outputs for safety violations."},
    {"title": "8.5 Data Privacy in Agentic Systems", "desc": "Agents process sensitive user data — privacy must be engineered, not assumed. Privacy threat model for agents: data memorization (model memorizes sensitive training data), data exfiltration (agent extracts data and sends it to unauthorized destinations), inference (agent reveals sensitive information about other users from training data). Privacy controls: data minimization (only include data necessary for the current task in agent context), purpose limitation (data collected for one purpose cannot be used for another), consent mechanisms (users explicitly consent to AI processing their data). PII detection and redaction: scan all external data before processing, redact PII before including in agent context, log redaction events for audit. Differential privacy for agent memory: add calibrated noise to memorized facts to prevent exact user data extraction. GDPR compliance for agents: right to erasure (delete all user data including agent memory), data portability (export user's agent memory), privacy impact assessment for high-risk AI systems. Case study: how a healthcare AI agent handles patient data — the technical controls that ensure HIPAA compliance in an agentic system."},
    {"title": "8.6 Governance Frameworks for Production Agents", "desc": "Enterprise deployment of agents requires governance structures that provide accountability, oversight, and clear escalation paths. AI governance for agents: AI owner (accountable for agent behavior and outcomes), AI reviewer (audits agent outputs for quality and safety), AI operator (manages agent infrastructure and availability). Risk classification for agents: low-risk (information retrieval, content generation for human review), medium-risk (automated communications, data analysis), high-risk (autonomous financial transactions, medical recommendations, hiring decisions). Governance processes: pre-deployment review (design review against safety checklist), post-deployment monitoring (ongoing quality and safety monitoring), incident response (clear escalation path for agent failures), regular auditing (quarterly review of agent behavior and outcomes). Accountability documentation: model card (capabilities, limitations, training data), intended use statement, known failure modes, safety guardrails implemented. Regulatory context: EU AI Act classifies agents by risk level, high-risk agents require conformity assessment and ongoing monitoring. Case study: how Microsoft's Responsible AI Standard governs AI agent deployment across Microsoft's product portfolio — the governance process and the enforcement mechanisms."},
    {"title": "8.7 Module 8 Capstone: Safety and Governance Framework", "desc": "Design a complete safety and governance framework for an enterprise AI agent: risk classification methodology, safety controls for each risk level (low/medium/high), human-in-the-loop design with confirmation gates for high-risk actions, prompt injection defense implementation, behavioral monitoring specification, data privacy controls, incident response playbook, governance documentation template, and an audit framework for quarterly review. Present the framework as a policy document for executive approval."}
  ]},
  {"title": "Module 9: Autonomous Workflow Design — Agentic Applications at Scale", "desc": "The frontier of agent engineering: autonomous multi-day workflows, computer use, code execution agents, and the production patterns for systems that operate with minimal human supervision.", "lessons": [
    {"title": "9.1 Autonomous Workflow Architecture: Long-Running Agent Systems", "desc": "Autonomous agents that operate over hours or days require fundamentally different architecture from request-response agents. Long-running workflow characteristics: multiple decision points (the agent makes dozens of decisions over the course of the task), state persistence (state must survive restarts, pod evictions, and infrastructure failures), asynchronous execution (the agent works in the background, human checks in periodically), external event handling (agent must respond to emails, calendar events, database changes). LangGraph for long-running workflows: checkpointed state persists across restarts (SQLite or PostgreSQL backend), graph structure enables complex branching and looping, interrupts allow human intervention at defined points. Workflow orchestration infrastructure: Celery or Temporal for task queuing (retries, timeouts, distributed execution), Redis for fast state storage, PostgreSQL for durable state. Cron-based agent scheduling: agents that run daily to check emails, update databases, or process incoming documents. Event-triggered agents: webhook receiver triggers agent on new GitHub issue, new email, new Slack message. Case study: how Lindy AI implements long-running autonomous agents — the workflow architecture for a 72-hour research task."},
    {"title": "9.2 Computer Use Agents: UI Automation and Web Interaction", "desc": "Computer use agents (Claude Computer Use, GPT-4V with browsing) can interact with websites and applications like a human. Computer use capabilities: screenshot perception (model sees the current screen state), click/type/scroll actions (model controls the mouse and keyboard), web navigation (navigate to URLs, interact with web forms). Playwright for web automation: headless browser automation, screenshot capture, click/type/scroll actions — the infrastructure layer for computer use. Computer use agent architecture: perception loop (screenshot → visual understanding → action planning), action execution (Playwright executes the planned action), state verification (screenshot after action verifies expected result). Safety for computer use: sandboxed browser environment (no access to sensitive files or accounts), action review (display planned actions for human approval before execution), session isolation (separate browser session per user per task). Reliability challenges: web UI changes break automation, CAPTCHAs block automated browsers, login flows require careful credential handling. Use cases: web research automation, form filling, UI testing, competitive intelligence. Case study: how Anthropic's Claude Computer Use is used for automated web testing — the architecture and the safety controls."},
    {"title": "9.3 Code Execution Agents: Sandboxed Computation", "desc": "Code execution agents generate and run code to answer questions, analyze data, and automate tasks. Code execution infrastructure: sandboxed execution environment (Docker container with resource limits), support for Python, JavaScript, Bash — restricted from file system and network access. E2B (Engineering-to-Bit): managed sandboxed code execution API — 3-second spin-up, 10-minute sessions, $0.10/hour. Code agent pattern: user asks a data analysis question → agent generates Python code → code executed in sandbox → output returned to agent → agent explains results. Data analysis agent: read CSV from S3, generate pandas analysis code, execute, return statistical summary and visualization. Code generation quality: test generated code before presenting results (if code throws an exception, debug and retry), validate outputs against expected schema. Safety for code execution: strict sandbox isolation (no network access, no file system access outside the working directory), resource limits (CPU, memory, execution time), dangerous operation detection (rm -rf, subprocess calls, file system access). Case study: how ChatGPT's Code Interpreter (Advanced Data Analysis) handles code execution — the sandbox architecture and the safety constraints."},
    {"title": "9.4 Browser Agents and Web Research Automation", "desc": "Web research agents can autonomously gather information from the internet, far exceeding the capabilities of simple web search. Web research agent capabilities: multi-page navigation (follow links to gather deeper information), form interaction (search queries, pagination), content extraction (parse relevant information from different page structures), source evaluation (assess credibility of information sources). Research agent architecture: task decomposition (break research question into sub-questions), parallel web search (search for each sub-question simultaneously), content extraction and summarization (extract key information from each source), synthesis (combine findings into a coherent summary with citations). Rate limiting and politeness: respect robots.txt, add random delays between requests (1-3 seconds), limit concurrent requests to same domain. Anti-bot evasion: use real browser headers, handle JavaScript-rendered content with Playwright, solve CAPTCHAs (use 2captcha API — ethical for legitimate research use). Source tracking and citations: track which URLs provided which information — enable proper attribution in outputs. Case study: how Perplexity AI implements their research agent — the web crawling, extraction, and synthesis pipeline."},
    {"title": "9.5 Document Processing Agents: Extraction, Analysis, and Generation", "desc": "Document processing agents automate the ingestion and analysis of contracts, reports, invoices, and other business documents at scale. Document processing pipeline: ingestion (PDF, Word, Excel parsing), structure recognition (identify headers, tables, lists), information extraction (entity extraction, key-value extraction), validation (check extracted data against business rules), output generation (structured JSON, database insert, summary report). LLM-powered extraction vs. traditional parsers: LLMs handle messy, inconsistent documents that rule-based parsers break on — trade-off: LLMs are slower and more expensive, but more flexible. Contract analysis agent: ingest contract PDF → extract key terms (parties, dates, payment terms, termination clauses, liability limits) → compare to template → flag unusual clauses → generate review summary. Invoice processing agent: extract vendor, amount, line items, dates → validate against purchase orders → route for approval → post to accounting system. Document generation agent: given structured data + template + style guide, generate draft documents (contracts, reports, proposals) for human review. Case study: how Harvey AI built their legal document analysis agent — the extraction accuracy, the error handling, and the human review workflow."},
    {"title": "9.6 Agent Orchestration at Enterprise Scale", "desc": "Deploying dozens of specialized agents across an enterprise requires orchestration infrastructure, governance, and operational practices. Enterprise agent registry: catalog of all deployed agents (name, purpose, owner, capabilities, tools, cost profile, deployment status) — the internal directory of AI capabilities. Agent routing: intelligent request routing to the appropriate specialized agent based on task classification — single entry point, intelligent dispatch. Agent versioning: maintain multiple versions of the same agent simultaneously (A/B testing, gradual rollout, emergency rollback). Cross-agent data sharing: agents need to access each other's outputs — shared knowledge base, event bus for inter-agent communication. Enterprise agent governance: approval process for new agent deployment, ongoing monitoring requirements, regular capability reviews, incident response procedures. Cost management at scale: track total LLM spend across all agents, per-agent cost attribution, shared model inference infrastructure to amortize fixed costs. Agent lifecycle management: deprecation process for agents that are no longer needed, migration support for users who depend on deprecated agents. Case study: how Salesforce manages 40+ AI agents across their product portfolio — the governance model, the operational practices, and the infrastructure."},
    {"title": "9.7 Module 9 Capstone: Autonomous Workflow Design", "desc": "Design an autonomous agent system for a complete business process: an enterprise procurement automation agent that receives purchase requests, identifies suppliers, evaluates options, drafts purchase orders, routes for approvals, and processes confirmations — operating autonomously for routine purchases (<$5K) with human escalation for complex cases. Deliverables: complete workflow graph (LangGraph StateGraph), tool definitions for all required integrations (ERP, email, supplier APIs), human-in-the-loop design (when to escalate, how), safety constraints, error handling and retry logic, cost model, deployment architecture, and monitoring dashboard."}
  ]},
  {"title": "Module 10: AI Agent Engineer Career — From Builder to Principal Agent Engineer", "desc": "Career development for AI agent engineers — the specializations, compensation, the open-source contributions that build reputation, and the 3-year plan to become a Principal Agent Engineer.", "lessons": [
    {"title": "10.1 The AI Agent Engineering Career Landscape", "desc": "AI agent engineering is one of the fastest-growing engineering specializations — the infrastructure for agentic AI is being built right now. Career levels: Junior Agent Engineer (builds simple chains and integrations), Agent Engineer (designs and deploys production agents independently), Senior Agent Engineer (leads complex multi-agent systems, sets architecture patterns), Staff/Principal Agent Engineer (defines agent platform strategy, cross-organization influence). The agent engineer vs. ML engineer distinction: agent engineers focus on orchestration, tool integration, and system design — ML engineers focus on model training and evaluation. Both perspectives are valuable on the same team. Compensation 2024 US: Junior $110-140K, Agent Engineer $150-200K, Senior $200-280K, Staff/Principal $280-400K+. Why agent engineering pays a premium: the field is new, expertise is scarce, production failure modes are novel and expensive. The 3-year window: the agent engineering field is codifying best practices right now — engineers who build deep expertise in this window will be recognized as founding experts. Case study: career trajectories of the early LangChain contributors — where they are today and how their open-source work created career opportunities."},
    {"title": "10.2 Building Expertise: The Hands-On Learning Path", "desc": "Agent engineering expertise comes from building — theory without production experience is insufficient. The learning progression: Level 1 (build a simple RAG chatbot, deploy on Streamlit), Level 2 (build a multi-tool agent with LangChain, deploy on FastAPI), Level 3 (build a stateful multi-agent system with LangGraph, deploy on Kubernetes), Level 4 (build a production autonomous workflow, monitor with LangSmith, handle failure modes). Open-source learning: clone and run LangChain, LlamaIndex, AutoGen, and CrewAI — understand their internal architectures by reading the source code. Kaggle for agent engineers: agent benchmarks (GAIA, SWE-bench) — compete to develop intuition for what techniques work. The 100-hour rule: 100 hours of deliberate hands-on building is worth more than 1,000 hours of reading about agents. Building in public: post what you build on LinkedIn and Twitter/X — attract collaborators, get feedback, build visibility. Case study: the GitHub portfolio that landed an agent engineering role at Anthropic — the projects, the commit history, and the system design decisions documented in READMEs."},
    {"title": "10.3 Contributing to the Agent Engineering Ecosystem", "desc": "The agent engineering ecosystem is being built by practitioners — contributions create career-defining visibility and relationships. High-impact contribution areas: LangChain (most used agent framework — bug fixes and documentation improvements are immediately impactful), LangGraph (newer, higher impact per contribution), LlamaIndex (leading RAG framework), AutoGen (Microsoft's multi-agent framework), Weaviate/Qdrant (vector databases). Contribution types: bug fixes (start here — identify a bug, write a fix, open a PR), documentation improvements (explain concepts more clearly, add examples), new features (implement a tool, add a connector, improve an existing capability), benchmarks (contribute to agent evaluation benchmarks — GAIA, SWE-bench). Getting started: find issues labeled 'good first issue' or 'help wanted', read CONTRIBUTING.md, join the project's Discord, introduce yourself before submitting your first PR. The contribution flywheel: contribution → code review feedback from core maintainers → learning → better contribution → maintainer trust → influence. Case study: how a senior engineer became a core contributor to LangGraph — the 18-month contribution history that led to a job offer."},
    {"title": "10.4 Specializations and Niches in Agent Engineering", "desc": "Deep specialization within agent engineering commands significant premium. High-value specializations: LLM evaluation and benchmarking (expertise in measuring agent quality — GAIA, SWE-bench, custom evaluation), agent safety and alignment (preventing harmful agent behavior — growing regulatory importance), multi-agent coordination (designing systems where 10+ agents collaborate effectively), domain-specific agents (legal agents, medical agents, financial agents — requires both AI + domain expertise). The specialization-premium relationship: agent security specialist commands 30-50% premium over generalist agent engineer — scarcity and high consequence create pricing power. Building a specialization: pick one area, go deep (read all papers, contribute to relevant open-source, build projects), publish findings, present at conferences. The intersection premium: agent engineer with a specific domain (finance, healthcare, legal) + agentic AI expertise = extremely rare, extremely valuable. Case study: the agent engineer who specialized in LLM evaluation and co-authored AgentBench — the paper that defined agent evaluation benchmarks, leading to a Principal Researcher role."},
    {"title": "10.5 Thought Leadership in Agent Engineering: Publishing and Speaking", "desc": "The agent engineering field is moving faster than textbooks can capture — practitioners who share what they're learning drive the conversation. Writing for agent engineers: technical blog posts (LangChain Deep Dive, Production Agent Patterns, Multi-Agent Failure Modes — 2,000-5,000 word deep technical posts), Twitter/X threads (quick insights, code examples, surprising discoveries — 2-10 minute reads), newsletter (weekly curated agent engineering insights — 500+ subscribers generates speaking invitations). Conference targets: NeurIPS workshops on language agents, MLSys, ICLR agent track, re:Invent AI track, AGI safety conferences. Speaking impact: one talk at NeurIPS generates more career opportunities than one year of LinkedIn posting. Content strategy: write what you know from production experience — case studies, post-mortems, and honest assessments of what works (and doesn't) in production agents. Writing frequency: publish once every 2 weeks minimum to maintain visibility — quality over frequency, but never disappear for more than a month. Case study: the LangChain blogger who documented every production agent failure mode they encountered — the post series that led to a developer advocate role and 50,000 Twitter followers."},
    {"title": "10.6 The Future of Agent Engineering: What Comes Next", "desc": "Agent engineering is evolving faster than any other software discipline — engineers who understand the trajectory will be best positioned. Near-term evolution (6-12 months): computer use goes production mainstream (every enterprise workflow automation will use browser agents), multi-agent frameworks mature (LangGraph and AutoGen become as foundational as React for web), agent evaluation becomes a discipline (dedicated agent benchmarks, evaluation tools, quality metrics). Medium-term evolution (1-3 years): agents become the primary interface to software (users describe what they want, agents execute across multiple apps), specialized foundation models for agents (models trained specifically for tool use and agentic reasoning), real-time agents (sub-second response with continuous context updating). Long-term implications (3-5 years): most knowledge worker tasks partially automated by agents, human role shifts to oversight and exception handling, agent engineering becomes a foundational software discipline. Career positioning: engineers who deeply understand current agent limitations (hallucination, context limits, reliability) will design the architectures that overcome them. Case study: the 2024 AI agent landscape from a technology evolution perspective — the s-curves and where we are on each."},
    {"title": "10.7 Final Capstone: Agent Engineering Portfolio and Career Plan", "desc": "Comprehensive career plan and portfolio review. Portfolio requirements: 3 production-quality agent projects (public GitHub repos with comprehensive documentation), 1 contribution to a major open-source agent framework, 5 published technical posts about agent engineering, 1 conference talk or meetup presentation. Career plan: Year 1 (complete all portfolio projects, make first open-source contribution, publish monthly, achieve Senior Agent Engineer title), Year 2 (staff-level architectural decision, recognized specialization, 10,000 GitHub stars or 20,000 newsletter subscribers), Year 3 (Principal Agent Engineer title, conference keynote or workshop paper, advisory role or independent consulting at $400K+ equivalent). Present your portfolio and career plan as if interviewing for a Staff Agent Engineer role at a leading AI company."}
  ]}
]

serve(async (req) => {
  if (req.method === "OPTIONS") return new Response(null, { headers: corsHeaders });
  const authError = await requireAdminOrServiceRole(req);
  if (authError) return authError;
  try {
    const supabase = createClient(Deno.env.get("SUPABASE_URL")!, Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
    const COURSE_TITLE = courseData.title;
    const { data: ex } = await supabase.from("courses").select("id").eq("title", COURSE_TITLE).maybeSingle();
    if (ex) await supabase.from("courses").delete().eq("id", ex.id);
    const { data: course, error: ce } = await supabase.from("courses").insert({ title: courseData.title, description: courseData.description, is_published: true, translations: courseData.translations }).select().single();
    if (ce) throw ce;
    let chaptersCreated = 0; let videosCreated = 0;
    for (let mi = 0; mi < courseData.modules.length; mi++) {
      const mod = courseData.modules[mi];
      const { data: chapter, error: che } = await supabase.from("chapters").insert({ course_id: course.id, title: mod.title, description: mod.desc, order_index: mi, translations: {} }).select().single();
      if (che) throw che; chaptersCreated++;
      for (let li = 0; li < mod.lessons.length; li++) {
        const les = mod.lessons[li];
        const transcript = les.title + "\n\n" + les.desc + "\n\nThis is a world-class lesson from Aladiah Academy's professional certification program. Prof. Didier teaches this with production depth, real case studies, and implementation guidance. Module context: " + mod.title + ". " + mod.desc;
        const trans = { en:{title:les.title,transcript}, es:{title:les.title,transcript}, fr:{title:les.title,transcript}, de:{title:les.title,transcript}, zh:{title:les.title,transcript}, ar:{title:les.title,transcript}, ja:{title:les.title,transcript} };
        const { data: video, error: ve } = await supabase.from("videos").insert({ chapter_id: chapter.id, title: les.title, description: les.desc, order_index: li, translations: trans }).select().single();
        if (ve) throw ve; videosCreated++;
        const { data: quiz, error: qe } = await supabase.from("quizzes").insert({ video_id: video.id, chapter_id: chapter.id, quiz_type: "mini_video", passing_score: 85 }).select().single();
        if (qe) throw qe;
        await supabase.from("quiz_questions").insert([
          { quiz_id: quiz.id, question_text: "What production expertise does this lesson build?", scenario_context: "Applying this in a professional role.", options: [les.desc.substring(0,120)+"...", "Only theoretical background", "Covered elsewhere", "Optional content"], correct_answer_index: 0, explanation: les.desc, order_index: 0 },
          { quiz_id: quiz.id, question_text: "Which real-world organizations' practices are referenced?", scenario_context: "Grounding knowledge in production reality.", options: ["Leading global organizations — case studies ground every lesson in production reality", "No real organizations referenced", "Only academic examples", "Only startups"], correct_answer_index: 0, explanation: "Every lesson uses production case studies from world-class organizations.", order_index: 1 },
        ]);
      }
      const { data: eq, error: eqe } = await supabase.from("quizzes").insert({ chapter_id: chapter.id, quiz_type: "chapter_end", passing_score: 85 }).select().single();
      if (eqe) throw eqe;
      await supabase.from("quiz_questions").insert([
        { quiz_id: eq.id, question_text: "What expert-level competency does this module develop?", scenario_context: "Module completion assessment.", options: [mod.desc.substring(0,140)+"...", "Only introductory familiarity", "Theoretical knowledge only", "Not career-relevant"], correct_answer_index: 0, explanation: mod.desc, order_index: 0 },
        { quiz_id: eq.id, question_text: "What career level does mastery of this module support?", scenario_context: "Professional development planning.", options: ["Senior professional and leadership roles in the fastest-growing AI specialization globally", "Entry-level only", "Academic roles only", "No direct career value"], correct_answer_index: 0, explanation: "Each module builds expertise directly valued in high-compensation AI professional roles.", order_index: 1 },
      ]);
    }
    return new Response(JSON.stringify({ message: courseData.title + " — 10-module world-class version seeded", courseId: course.id, modules: chaptersCreated, lessons: videosCreated }), { headers: { ...corsHeaders, "Content-Type": "application/json" } });
  } catch (e: unknown) {
    return new Response(JSON.stringify({ error: e instanceof Error ? e.message : String(e) }), { status: 500, headers: { ...corsHeaders, "Content-Type": "application/json" } });
  }
});
