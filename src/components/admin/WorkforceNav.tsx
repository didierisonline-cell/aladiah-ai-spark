// WorkforceNav is now an alias of the single founder navigation (FounderNav),
// which renders the shared FOUNDER_NAV_ITEMS. Kept as a re-export so existing
// admin-page imports keep working while there is ONE navigation renderer.
export { default } from '@/components/founder/FounderNav';
