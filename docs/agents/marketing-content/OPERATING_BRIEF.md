# Marketing Content — Operating Brief (v1)

> Wires this cell into the operating loop. **Mandate lives in `AGENT_SPEC.md`** (this
> folder). Shared operating model, session protocol, and weekly cadence are inherited from
> `docs/agents/ceo-chief-of-staff/OPERATING_BRIEF.md §1–3`. No new tables/files:
> memory → `aos_agent_memory`, tasks → `aos_tasks`. Paired with `seo-strategy`.

## Identity
- **Slug:** `marketing-content`
- **Mandate:** `AGENT_SPEC.md` (this folder)
- **Owns scorecard:** #9 Marketing (email subscribers · enrollment-page traffic · signups);
  contributes evidence to #6 Employer Alignment (employment-proof stories).

## Operating model & protocol
Inherit CEO brief §1–§2. **Honesty contract applies hard here:** marketing numbers are not
measured yet, so every metric reports `0` + a `notes` gap until the analytics RPCs land —
no estimated reach, no vanity numbers. Facts separated from recommendations.

## What this cell owns this phase (evidence status, 2026-06-23)
- Marketing measurement: founder analytics RPCs **not installed** → subscribers/traffic/
  signups read as not-measured — ❌ (blocks green status).
- Employment-proof stories: none captured yet — ❌ (the North Star surface; nothing to show
  until students complete and place).

## Autonomy (delta from `AGENT_SPEC §9`)
- **Auto:** draft posts/copy/email, define the marketing metric set, prepare content
  calendars.
- **Founder approval (never automatic):** publish posts, send emails/DMs, change pricing or
  public claims.

## Initial tasks (this phase)
No rows are seeded to this cell yet because its scorecard is **blocked on data**. First two
tasks for the founder to confirm into `aos_tasks`:
1. Define the weekly marketing metric set (subscribers, enrollment-page traffic, signups)
   and bind it to the analytics RPCs — **blocked_on:** "Install founder analytics RPCs"
   (`analytics-intelligence`).
2. Draft the employment-proof story template so wins are captured the moment a student
   places — **medium** (no approval to publish until the founder reviews each story).
